# أمر التنفيذ — Sting Systems (يُرسل إلى Claude Code كما هو)

> انسخ كل ما تحت هذا السطر إلى Claude Code، وافتح المستودع على مجلد فارغ يحتوي هذا الملف ومجلد `design_handoff_sting_systems/` بجانبه.

---

أنت تبني **Sting Systems**: نظام نقطة بيع وذمم ومخزون يعمل **بلا اتصال أولاً** لمحلات التجزئة الصغيرة في السودان، ومعه **سوق توريد B2B** للاكتشاف والطلب بين المحلات والموردين. المرجع الحاكم للمتطلبات هو الوثيقة `Sting-Systems-v21-Complete.md` (النسخة ٢١)، والمرجع الحاكم للواجهات هو حزمة التصميم في `design_handoff_sting_systems/` — ٤٩ ملف `*.dc.html` تفتح في المتصفح، مع `handoff/states-matrix.csv` و`handoff/coverage.csv` و`handoff/tokens.json` و`handoff/not-drawn.md` و`README.md`.

**ملفات الحزمة مراجع تصميم بصيغة HTML، لا كودَ إنتاج يُنسخ.** مهمتك إعادة بنائها بالتقنيات المعتمدة أدناه، بنفس التخطيط والألوان والخطوط والنصوص والحالات — دقّة عالية (hi-fi).

## ١ · التقنيات المعتمدة — لا بديل بلا قرار موثَّق

الأدوات والطبقات مصدرها §١٢.١ و§٤.٤ و§٤.٥ من الوثيقة. أما أرقام الإصدارات (Python 3.12، Django 5، PostgreSQL 16، Next.js 15، Tauri 2، TanStack v5، Expo أحدث مستقر) وأدوات الاختبار والمعرض (pytest، Vitest، Playwright، Storybook) فقرار هذا الأمر لا نصّ الوثيقة — سجّلها كأول ملف `docs/decisions/0001-versions-and-test-tools.md` قبل أي كود، ولا تُعاملها كنصّ من الوثيقة. لا تستبدل مكتبة ولا تضيف أخرى دون كتابة سبب في `docs/decisions/`.

| الطبقة | التقنية | الضابط |
|---|---|---|
| الخلفية | **Python 3.12 · Django 5 · Django REST Framework** | Modular Monolith بوحدات: `core/ sync/ parties/ catalog/ inventory/ sales/ purchasing/ notifications/ marketplace/` — لا خدمات مصغرة |
| قاعدة البيانات | **PostgreSQL 16** | RLS لعزل المستأجرين، قيود ومعاملات؛ الأرصدة إسقاطات مشتقة من الحركات |
| المصادقة | **JWT** (تجديد + إلغاء) | عقد §٩.٤؛ قفل محلي بـPIN على الجهاز لا يمرّ بالخادم |
| المهام | **Celery · Redis · Celery Beat** | للجدولة والإشعارات فقط؛ صحة البيع لا تعتمد عليها |
| الويب / PWA | **Next.js 15 (App Router) · TypeScript · Tailwind CSS · Service Worker** | Responsive: 390 / 834 / 1440؛ POS والإدارة وبوابة الزبون والسوق في تطبيق واحد |
| الموبايل | **React Native · Expo SDK (أحدث مستقر) · TypeScript · expo-sqlite · expo-secure-store** | لا تعيد استخدام DOM؛ مكوّنات RN أصلية بنفس الرموز |
| الديسكتوب | **Tauri 2 · React · Vite · TypeScript** يستورد `packages/ui-web` | SQLite محلي؛ Rust بالحدّ الأدنى للطباعة والملفات والأسرار |
| حالة الخادم | **TanStack Query v5** | للبيانات الخادمية فقط — ليست دفتر المبيعات المحلي |
| حالة الواجهة | **React Context** + حالة مكوّن | اللغة والمظهر والمستخدم والفرع |
| التخزين المحلي | **Dexie (IndexedDB)** للويب · **SQLite** للموبايل والديسكتوب | محوّلات بعقد واحد في `packages/platform` + اختبارات مشتركة |
| الأيقونات | **lucide-react** (ويب/ديسكتوب) · **lucide-react-native** | عائلة واحدة |
| العقود | **OpenAPI 3.1** مولَّد من DRF → `packages/contracts` (openapi-typescript) | مصدر الأنواع الوحيد |
| الاختبار | **pytest** · **Vitest** · **Playwright** (ويب) · متجهات اختبار مشتركة JSON للمال والتقريب | نفس نتيجة الحساب على Python وTypeScript |
| الخطوط | **Cairo** (600/700 للعناوين) · **IBM Plex Sans Arabic** (400–700 للنص) · **IBM Plex Mono** (للأرقام والمعرفات فقط) | تُحمَّل محلياً في الحزمة لا من CDN — التطبيق يعمل بلا إنترنت. المصدر `02-Design-System` و`handoff/tokens.json` (DS-1.2: `font.heading` = Cairo، `font.ui` = IBM Plex Sans Arabic بلا Noto). إن وجدت نسخة DS-1.1 تذكر Noto ولا تذكر Cairo فهي قديمة و`02-Design-System` هو الحاكم |

**ما ليس معتمداً:** Redux، خدمات مصغرة، تطبيق رابع، GraphQL، أي مكتبة UI جاهزة (MUI/Ant/Chakra) — المكوّنات تُبنى من الحزمة `packages/ui-web` وفق `02-Design-System.dc.html`.

## ٢ · هيكل المستودع (§٤.٤) — أنشئه تدريجياً لا حزماً فارغة

```
backend/              Django/DRF
apps/web/             Next.js — عام + إدارة + POS + PWA + بوابة زبون + سوق
apps/mobile/          Expo
apps/desktop/         Tauri + Vite
packages/contracts/   أنواع وعميل API من OpenAPI
packages/domain/      المال والكميات والتقريب والتحقق — TS بلا DOM
packages/sync-core/   بروتوكول المزامنة وانتقالات الحالات (§٨)
packages/platform/    عقود التخزين والأسرار والطباعة والإشعارات + محوّلات
packages/ui-web/      مكوّنات React DOM المشتركة (ويب + ديسكتوب)
packages/design/      الرموز من handoff/tokens.json + الترجمات + قواعد التصميم
docs/decisions/       كل انحراف عن الوثيقة أو الحزمة: سبب وتاريخ
```

## ٣ · قواعد التصميم غير القابلة للتفاوض

اقرأ `02-Design-System.dc.html` و`22-Rules-Register.dc.html` كاملين قبل أول مكوّن.

> **تنبيه تسمية:** `ACC-nn` في الحزمة هي مجموعة شاشات الدخول (`ACC-01…10`)، و`ACC-nn` في §١٨ من الوثيقة معيار قبول. في هذا الأمر يُكتب المعيار دائماً بصيغة «معيار §١٨ ACC-nn»، والشاشة بمعرفها المجرد. لا تخلط بينهما.

### القاعدة الأولى — الحاكمة: الاعتماد الكلي على الواجهات المصمَّمة في هذه الحزمة

**لا تصمّم شيئاً. كل شاشة وكل حالة وكل نصّ وكل لون وكل مقاس مصدره إطارٌ مرسوم في `design_handoff_sting_systems/`.** أنت تُنفّذ التصميم القائم، لا تقترح تصميماً ولا «تحسّنه» ولا تكمله من ذوقك أو من أنماط مكتبات أخرى. تحديداً:

- **الشاشات:** لا شاشة إلا ما له معرف في السجل (164 شاشة في `00-Coverage`). ولا شاشة في السجل تُبنى بغير إطارها.
- **الحالات:** كل زوج (شاشة × حالة) — 754 زوجاً — له إطار مرسوم واحد في `handoff/states-matrix.csv` عند **التركيبة الأساسية لمجموعته**: `W/1440` للمجموعات المتجاوبة، `M/390` لـNAT، `D/1366` لـDESK، `C/390` لـCUS، `A/1440` لـPLT (788 صفاً موسومة `drawn` ولها `frame_ref`). باقي الصفوف (3281 من 4069) بلا `frame_ref` **عمداً**: تركيبات عرض لنفس الحالة تتبع لوحة «ما يتغيّر» داخل شاشتها، لا إطاراً مستقلاً. تُبنى الحالة كما في إطارها الأساسي — لا حالة تُخترع ولا حالة تُسقَط. الحالة الفارغة والخطأ والانقطاع والصلاحية المرفوضة كلها مرسومة؛ لا تكتب لها نصاً من عندك.
- **النصوص:** رسائل الواجهة والعناوين والأزرار وأسباب الرفض تُنقل **حرفاً** من الإطار — بما فيها التشكيل والعلامات (« » · —). لا تُعاد صياغتها ولا تُختصر ولا تُترجم.
- **التخطيط والمقاسات:** الأعمدة والعروض والهوامش والأحجام كما في الإطار وفي `handoff/tokens.json`. تركيبات العرض غير المرسومة تتبع لوحة «ما يتغيّر» داخل شاشتها، والأسطح المختلفة (تابلت/موبايل/ديسكتوب/بوابة/ويب عام) تتبع `46-D37` و`47-D38` — لا اجتهاداً. **أسطح D37/D38 (S-01…S-06، ومنها تابلت POS `W/POS-01/834`) غير موسومة في المصفوفة؛ مرجعها الملفان 46 و47 مباشرة.**
- **الألوان والخطوط والمكوّنات:** من `02-Design-System` و`23-Handoff` (المكوّنات الـ٢٩) فقط. لا لون خارج الرموز، ولا خط خارج الثلاثة، ولا مكوّن من مكتبة جاهزة.
- **السلوك:** `48-Sting-App.dc.html` مرجع السلوك لقلب النظام (وردية → بيع → ذمّة → انقطاع → مزامنة). ما يفعله هو الصواب؛ ما يخالفه خطأ.
- **عند الغموض أو النقص:** لا تُكمل الفراغ بتصميمٍ منك. توقّف، سجّل الموضع في `docs/decisions/` بصيغة «الوثيقة تطلب X والحزمة لا تحمل إطاراً له»، واسألني. **الاستثناء الوحيد** لسدّ الفراغ هو نصّ الوثيقة الحرفي إن كان صريحاً — وحتى عندها تُسجَّل الإضافة للمراجعة.
- **معيار القبول لكل شاشة:** اختبار Playwright يفتح كل حالة. النصوص الحرفية تُطابَق في **كل مقاس**؛ الأنماط (اللون والخط والمسافات والتخطيط) تُطابَق عند **المقاس الأساسي** بقيم الرموز المحسوبة (`getComputedStyle` مقابل `tokens.json`)، لا بمقارنة بكسلية مع ملف `dc.html` — تلك الملفات تُعرض بمنظومة `support.js` الخاصة ولا تصلح مرجعاً بكسلياً. لقطات الشاشة تصبح مرجعاً بعد أن أعتمدها أنا عند أول مطابقة يدوية. الشاشة التي لا تطابق إطارها **غير مكتملة** ولو عملت.

القواعد التالية تفصيلٌ لهذه القاعدة لا إضافةٌ عليها:

1. **RTL أولاً.** `dir="rtl"` على الجذر، وخصائص منطقية (`inline-start`) لا `left/right`. الشريط الجانبي في الجانب الابتدائي.
2. **الأرقام:** لاتينية بعرض ثابت في **IBM Plex Mono** مع `direction:ltr; unicode-bidi:isolate` — للمبالغ والكميات والمعرفات فقط. **لا حرف عربي داخل عنصر mono أبداً**؛ الكلمة العربية في span مجاور بخط النص.
3. **سجل الحالات سبع عشرة حالة** (`ready loading empty validation_error permission_denied offline stale saving saved_local pending_sync synced conflict server_error expired partial success phase_locked`)؛ كل شاشة تحمل **مجموعتها المسجلة منها فقط** في `states-matrix.csv` (754 زوجاً، نحو خمس حالات للشاشة؛ `POS-11` مثلاً بلا `ready`) — لا تُضاف حالة إلى شاشة ولا تُسقَط، وكل زوج مسجَّل له إطار يُطابَق بالنص.
4. **`saved_local` للنماذج و`pending_sync` للقوائم** — لا تُخلطان.
5. **الرصيد يُعرض مركّباً** عند وجود معلّق: «خادمي X + معلّق هذا الجهاز Y = Z» — لا مجموع وحده.
6. **النجاح بعد الحفظ لا بعد الطباعة.** فشل الطابعة لا يلغي بيعاً؛ إعادة الطباعة «نسخة» بنفس الرقم.
7. **النقد وحده في الصندوق.** الدفع المختلط ثلاث وجهات معلنة (نقد → صندوق، آجل → ذمّة، تحويل → «مسجَّل — غير مطابق»).
8. **لا حذف لأصل.** المرتجع والتصحيح والدمج مستندات مستقلة تشير إلى أصلها.
9. **المتوقَّع محجوب حتى يُدخل المعدود** — في إقفال الصندوق وفي الجرد.
10. **هدف اللمس ≥ 44px** على كل منصة (46px على تابلت POS).
11. **`phase_locked`** لوظائف M3/M4 وما يحتاج عقد تكلفة (G-03): مصمَّمة ومعطَّلة بسبب صريح — لا تُخفى ولا تُبنى عاملة.
12. **لا افتراض تطابق التنقل بين المنصات** (§١٤.٦): الأسطح الستة في `46-D37` و`47-D38` مرجع تخطيط الموبايل والتابلت والديسكتوب.

## ٤ · ترتيب البناء — ملزم (§١٥.١)

**الويب/PWA أولاً وحتى النهاية، ثم Expo، ثم Tauri.** لا تبدأ منصةً قبل إغلاق بوابة السابقة **وموافقتي الكتابية على الانتقال**. داخل كل تسليم: العقود → المجال → التخزين المحلي → الواجهة → الاختبار.

### المرحلة ٠ — الأساس (قبل أي شاشة)
1. المستودع بالهيكل أعلاه، CI (pytest + Vitest + Playwright + lint + typecheck).
2. `packages/design`: استورد `handoff/tokens.json` (DS-1.2) → Tailwind config + متغيرات CSS. README الحزمة يصف الملف «مرجعاً للقيم لا مصدراً يُولَّد منه CSS»؛ التوليد منه قرار هذا الأمر — سجّله في `docs/decisions/`، وأي قيمة يخالف فيها الملف `02-Design-System` يحكمها الأخير وتُصحَّح في الملف. الخطوط الثلاثة محلياً (رخصة OFL).
3. `packages/domain`: المال بالقروش (integers)، التقريب المصرفي (معيار §١٨ ACC-24)، الوحدات وتحويلها (معيار §١٨ ACC-19/20). متجهات اختبار مشتركة تُشغَّل من Python وTS.
4. `backend/core` + `sync`: المستأجر، الفرع، الجهاز، المستخدم، الدور، عقد PUSH/PULL (§٨.٣–٨.٧)، `operation_id` ومنع التكرار، عداد المستأجر واللقطات.
5. `packages/sync-core` + `packages/platform`: عقد التخزين، محوّل Dexie، طابور العمليات، حالات `local → pending → synced/conflict`.
6. `packages/ui-web`: المكوّنات الـ٢٩ (`C-FRAME` … `C-PHASE`) كما في `23-Handoff.dc.html` — كل مكوّن بحالاته و Storybook أو صفحة معرض.

### المرحلة ١ — الويب: النواة ودورة الأعمال (P0)
بالترتيب، وكل شاشة بكل حالاتها من المصفوفة:
1. `ACC-01…10` الدخول والتهيئة والتنزيل الأول والقفل بـPIN.
2. `HOME-01…03`.
3. `CAT-01…06` الكتالوج والوحدات والباركود.
4. `SHIFT-01…05` الورديات — قبل POS لأن البيع النقدي يشترط وردية.
5. `POS-01…12` البيع: السلة، الوحدة/الوزن، الخصم بسقوف الدور، العميل، نقدي/آجل/مختلط، الإيصال، الفواتير، المرتجع، فشل الحفظ/الطباعة، مراجعة التكرار. **السيناريو الموحَّد ١٠٠ = ٤٠ نقداً + ٦٠ آجلاً يجب أن يظهر في POS-07 وSHIFT-02 وPTY-05 بنفس الأرقام (معيار §١٨ ACC-08).**
6. `PTY-01…09` الأطراف والكشف والسداد والدمج وقالب تذكير الدين اليدوي (داخل PTY-08).
7. `INV-01…10` المخزون والجرد الأعمى والهالك والتحويلات.
8. `SYS-01…11` مركز المزامنة والتعارض والنسخ والاستعادة والتشخيص.
9. `WEB-01…03` تثبيت PWA وإذن Web Push وربط طابعة الويب.
**بوابة الخروج:** جهازان، انقطاع وعودة، بيع/سداد/مرتجع من جهازين، طباعة فاشلة، استعادة نسخة — بلا فقد ولا تكرار (معايير §١٨ ACC-01…ACC-87 ذات الأثر المرئي).

### المرحلة ٢ — الويب: الإدارة والتقارير والرسائل
`ORG-01…10` · `REP-01…06` (بلا عمود تكلفة — `phase_locked`) · `NOT-01…06` · `PUR-01…05` (مشروط — `phase_locked`) · `PUB-01…04`.

### المرحلة ٣ — الويب: بوابة الزبون والسوق M1/M2
1. `CUS-01…05` على 390 و1440 (`47-D38` S-05).
2. `MP-01…15` السوق: الملف العام أولاً على 390 (`47-D38` S-06)، المقارنة بوحدة واحدة (لا «الأرخص» بوحدتين — معيار §١٨ ACC-122)، صيغتا النشر (سعر / توفر فقط)، التحقق المنفصل للبائع (G-06 المسار ب).
3. `ORD-01…15` الطلبات: سلّم الكميات (مطلوب/مؤكَّد/مشحون/مستلم)، الذمّة من المستلم وحده (معيار §١٨ ACC-127)، النسخة المقبولة هي الاتفاق (معيار §١٨ ACC-125/145)، الإعادة بنفس المعرف (معيار §١٨ ACC-124).
4. `LINK-01…05` و`GROW-01…03`: واجهات كاملة بحالة `phase_locked` — M3/M4.
5. `PLT-01…12` إدارة Sting على 1440، وسطح القرار على 390 (`47-D38` S-04).
**بوابة الخروج:** MK-1 وMK-2 وMK-4 كما في بوابة المرحلة ج (§١٥.١)؛ MK-3 (عدم تكرار المال والاستعادة) شرط تفعيل M3 لا شرط إغلاق هذه المرحلة، لكن اختباراتها تُكتب مع ORD. والمعايير §١٨ ACC-118…150.

### المرحلة ٤ — الموبايل Expo
**لا تبدأ هذه المرحلة إلا بموافقتي الكتابية.** §١٥.١ تضع بين الويب وExpo مرحلة «د: شهر تشغيل فعلي يتضمن فترة ذروة»؛ هذه بوابة بشرية ميدانية لا آلية، ولا يغلقها اختبار.
نفس API ونفس `packages/domain` و`sync-core` و`contracts`. واجهات RN أصلية وفق `46-D37` S-02 (تنقّل سفلي بأربعة أبواب، الرقم هو الشاشة، الكاميرا مدخل أول). `NAT-01…04`. SQLite + SecureStore + طباعة Bluetooth.
**بوابة الخروج:** بيع من الويب وسداد ومرتجع من الموبايل مع انقطاع وعودة؛ ترقية واستعادة مجرَّبتان.

### المرحلة ٥ — الديسكتوب Tauri
Vite + React يستورد `ui-web`. `DESK-01…05` و`47-D38` S-03 (ثلاثة أعمدة، صفّ 36px، تحديد متعدد، اختصارات) على المقاسين `1366` و`1920` (تغيير حجم النافذة بين المقاسين مطلوب — `tokens.json` platform.D). SQLite + طباعة أصلية + مثبِّت وتحديث.
**بوابة الخروج:** المنصات الثلاث معاً بلا اختلاف حساب أو تسرب أو تكرار.

## ٥ · كيف تقرأ الحزمة

- `00-Coverage.dc.html` الفهرس والسجل الكامل (164 شاشة × حالاتها).
- `handoff/states-matrix.csv` الحقيقة الآلية: لكل صف `screen_id · platform · viewport_px · state_code · design_status · frame_ref` — الـ`frame_ref` (في الصفوف الموسومة `drawn`) هو الملف والمقطع الذي ترسم منه؛ الصفوف بلا `frame_ref` تركيبات عرض تُوصف في لوحة «ما يتغيّر».
- `handoff/coverage.csv` صف لكل شاشة: حالاتها ومكوّناتها ومساراتها (`flows`) ومعايير قبولها ومرحلتها (`أساسي / M1 / M2 / M3 / M4 / مشروط`)؛ `11-Prototype-Flows.dc.html` المسارات `F01…F17` التي تربط الشاشات برحلة واحدة.
- **الأرقام:** `states-matrix.csv` هو المصدر الوحيد للعدّ (`handoff/sync-figures.md`). لا تستشهد بأرقام واردة في نثر الملفات الأخرى إن خالفت المصفوفة.
- الدفعات `03…43` الإطارات بكل حالاتها؛ `44` تدقيق المطابقة بالوثيقة؛ `45` نواقص مغلقة؛ `46/47` الأسطح الستة؛ `48-Sting-App.dc.html` نموذج عامل لقلب النظام — **استخدمه كمرجع سلوك** (المنطق فيه صحيح: وردية → بيع → ذمّة → انقطاع → مزامنة).
- `README.md` داخل الحزمة يحمل القائمة الكاملة للقرارات المحسومة G-01…G-16 وجداول الشاشات بحالاتها ومكوّناتها؛ `13-D8-Decisions-Applied.dc.html` يعرض المطبَّق منها على الشاشات فقط (G-01، G-02، G-08، G-09، G-10، G-11)، و`44-Spec-Audit-v21` يحيل إلى G-03/G-06/G-15؛ `30-Decisions-States-Registry.dc.html` قرارات سجل الحالات.

## ٦ · ما لا تفعله

- لا تخترع شاشة ضريبية أو حقل رقم ضريبي (G-11 معلَّق على مراجعة قانونية — PUB-02 قالب بفتحات).
- لا تبني حساب تكلفة أو هامش (G-03) — `phase_locked` مع سبب.
- لا تعرض «عمر الدين» ولا جدول أعمار (G-15) — «آخر سداد» و«منذ» فقط.
- لا تُنشئ طلب سوق بديلاً عند فقد الرد — استعلام ثم إعادة بنفس المعرف.
- لا تجعل نجاح HTTP شرطاً لأي حفظ محلي.
- لا تقرّب في الواجهة — التقريب في `packages/domain` وحدها.
- لا تستخدم Noto أو Inter أو Roboto — الخطوط الثلاثة المذكورة فقط.

## ٧ · طريقة العمل المطلوبة منك

0. **قبل كل شيء:** أكّد لي كتابةً أنك قرأت القاعدة الحاكمة في القسم ٣ وأنك ستنفّذ الواجهات المصمَّمة في الحزمة كما هي — بلا تصميم من عندك ولا إكمال فراغ بالاجتهاد.
1. ابدأ بقراءة الوثيقة كاملة ثم `00-Coverage` و`02-Design-System` و`22-Rules-Register` و`23-Handoff`، ثم اكتب `docs/PLAN.md` بتقسيم المرحلة ٠ و١ إلى مهام لا تتجاوز يوماً، وانتظر موافقتي قبل الكود.
2. لكل شاشة: افتح `frame_ref` من المصفوفة، وابنِ الحالات كلها، وأضف اختبار Playwright يمرّ على كل حالة ويقارن النص الحرفي للرسائل بالإطار.
3. أي غموض بين الوثيقة والحزمة: الحزمة أحدث في التصميم والوثيقة أحدث في الأعمال؛ سجّل الخلاف في `docs/decisions/` واسألني.
4. بعد كل تسليم: تقرير قصير بما بُني، وما اختُبر، وما بقي، بلا تقديرات زمنية غير مقاسة (§١٥.١).

ابدأ بالخطوة ٠ من القسم ٧.
