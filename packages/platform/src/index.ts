/**
 * @sting/platform — عقود (منافذ) المنصة: التخزين الذرّي، الأسرار، الطباعة، الإشعارات، الوقت، الهوية.
 * المحتوى الفعلي في T0.10 وT0.11. محوّل Dexie يعيش هنا لكن النواة لا تستورده مباشرة.
 */
export interface Clock {
  /** وقت الجهاز — لا يُعامل مرجع ترتيب موثوقاً (§٥.٢). */
  now(): Date;
}

export const systemClock: Clock = { now: () => new Date() };
