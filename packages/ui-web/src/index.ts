/**
 * @sting/ui-web — المكوّنات الـ29 كما في 23-Handoff.dc.html. تُبنى من T0.14.
 * dir="rtl" على الجذر وخصائص منطقية فقط (القاعدة 1 من الأمر).
 */
import { DESIGN_SYSTEM_VERSION } from "@sting/design";

export const UI_WEB_DESIGN_SYSTEM = DESIGN_SYSTEM_VERSION;
