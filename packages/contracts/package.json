{
  "name": "@sting/contracts",
  "version": "0.0.0",
  "private": true,
  "description": "أنواع وعميل API مولَّدان من OpenAPI — لا تُحرَّر المخرجات يدوياً (T0.9)",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./schema": "./src/generated/schema.ts"
  },
  "scripts": {
    "generate:schema": "cd ../../backend && uv run python manage.py spectacular --file ../packages/contracts/openapi.json --format openapi-json",
    "generate:types": "openapi-typescript openapi.json -o src/generated/schema.ts",
    "generate": "pnpm generate:schema && pnpm generate:types",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "openapi-fetch": "^0.13.0"
  },
  "devDependencies": {
    "openapi-typescript": "^7.4.0",
    "typescript": "^5.6.0"
  }
}
