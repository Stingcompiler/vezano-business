/**
 * @sting/contracts — يُولَّد من backend عبر drf-spectacular → openapi-typescript (T0.9).
 * أي تعديل يدوي هنا يُكتب فوقه عند التوليد.
 */
export const CONTRACTS_SOURCE = "openapi.json" as const;
