/**
 * @sting/domain — نواة الحسابات المشتركة.
 * لا تستورد React ولا DOM ولا مشغّل منصة (§٤.٦ بند ١). المال والكميات بـ BigInt (§٦.١).
 * المحتوى الفعلي في T0.3 وT0.4؛ هذا المدخل يثبت خط الفحص فقط.
 */
export const DOMAIN_CONTRACT_VERSION = 1 as const;

/** التعبير القانوني للعدد الصحيح كما في §٦.١ — يُستكمل بالتحقق الكامل في T0.3. */
export const INTEGER_STRING = /^(0|[1-9][0-9]*|-[1-9][0-9]*)$/;
