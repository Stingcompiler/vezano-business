from django.apps import AppConfig


class CoreConfig(AppConfig):
    name = "core"
    verbose_name = "المستأجرون والفروع والأجهزة والمستخدمون"
    default_auto_field = "django.db.models.BigAutoField"
