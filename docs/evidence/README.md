# ملف الأدلة — بوابة المرحلتين ١ و٢ (§١٨.٤)

لكل معيار: الاختبار، commit، بيانات الاختبار، الجهاز، النتيجة، العيوب المفتوحة. **نجاح فحص الوثيقة لا يسجّل نجاحاً لأي ACC** — يسجَّل هنا ما ثبت آلياً، وما ثبت يدوياً بجهازه، وما لم يُثبت بعد.

## البيئة الآلية
- الاختبار: `apps/web/e2e-gate/*.spec.ts` بـ`pnpm --filter @sting/web e2e:gate` (Playwright، سياقان = جهازان، Chromium بلا رأس @1440) على **خادم Django حقيقي** (PostgreSQL 16، `STING_ENV=ci`، `STING_FAULTS_ENABLED=1`) — لا `page.route`. Next يمرّر `/api/*` إلى Django بنفس الأصل (`STING_API_UPSTREAM`).
- الحالة الابتدائية (§١٥.٤) قبل كل تجربة بـ`POST /api/scenario/reset`: عميل «أحمد الطيب — تجريبي» برصيد صفر وحدّ ائتمان 1,000.00؛ «سكر» 10 كغ (افتتاحي معتمد) بسعر 100.00/كغ؛ الصندوق صفر (وردية تُفتح بـ0.00)؛ جهازان يُسجَّلان من المتصفح فعلاً (تسجيل + تنزيل أول).
- المحاكاة المعزولة عن الإنتاج: `drop_ack` (الخادم يطبّق ويلتزم ثم يخفي الردّ)، `freeze_reconciliation`/`network_cut` (الخادم لا يطبّق شيئاً)، `printer_fail`، `verify_send_fail`، `sms_*` (مزوّد الرسائل)، `po_send_fail`، `maintenance` (إعلان صيانة على صفحة الحالة)؛ الاشتراك بـ`POST /api/scenario/subscription {state, days_since_expiry, plan_code}` والإذن التسويقي بـ`POST /api/scenario/marketing`. قطع الشبكة على الجهاز = إسقاط `/api/*` وإعلان «بلا اتصال» (خادم Next التطويري يخدم أجزاء المسارات عند الطلب فلا `setOffline`).
- ما يُحاكى في المتصفح فقط: منح التخزين الدائم (`navigator.storage.persist`) لأن Chromium بلا رأس لا يمنحه لأي موقع — المنح الحقيقي يُتحقق يدوياً على الجهاز.
- التشغيل: CI job `gate` في `.github/workflows/ci.yml`؛ محلياً يحتاج قاعدة `sting_gate_test` (`createdb sting_gate_test`).

## سجل المعايير ذات الأثر المرئي

| المعيار | التجربة (§١٥.٤) | الاختبار الآلي | الجهاز/البيئة | النتيجة | العيوب المفتوحة |
|---|---|---|---|---|---|
| ACC-02، ACC-96 | جهازان دون شبكة: 100 + 60 → 160 بلا تكرار | `gate-two-devices.spec.ts` «ACC-02/96» | Chromium بلا رأس × 2 + Django | **يمرّ آلياً** — فاتورتان مؤكدتان، الكشف 160.00 على الجهازين | لا PULL للويب بعد (الجهاز يرى حركات غيره من الخادم لا من التنزيل التزايدي) |
| ACC-05 | انقطاع الرد بعد الحفظ (فقد ACK) → الرصيد يبقى 100 | «ACC-05» | نفسه | **يمرّ آلياً** — إعادة الرفع تُرفض تكراراً بالهوية | — |
| ACC-09 | بيع نقدي 100 ثم مرتجع صالح كامل نقداً → مخزون 10 وصندوق 0 وذمة 0 | `gate-money.spec.ts` «ACC-09» | نفسه | **يمرّ آلياً** | — |
| ACC-08 | بيع مختلط 40 نقداً + 60 آجلاً → مخزون 9 وصندوق 40 ودين 60 | «ACC-08» | نفسه | **يمرّ آلياً** قبل المزامنة وبعدها | — |
| ACC-84 | فشل الطباعة ثم إعادة طباعة → بيع واحد ونفس الرقم ونسخة معلّمة | «ACC-84» | نفسه (الطابعة محاكاة `printer_fail`) | **يمرّ آلياً** | الطابعة الفعلية: انظر `printers.md` (ACC-83 غير مُثبت) |
| ACC-07 | تجميد المصالحة ثم بيع محلي إضافي → المجموع لا يتضاعف ولا يختفي | `gate-recovery.spec.ts` «ACC-07» | نفسه | **يمرّ آلياً** | — |
| ACC-73 | استعادة نسخة مرتين على جهاز بديل → لا تكرار، الأصل باقٍ، هوية جديدة للكتابات الجديدة | «ACC-73» | نفسه | **يمرّ آلياً** | — |
| ACC-63، ACC-45 | موظف معطل ثم دخول آخر على جهاز مشترك → لا محو لعمليات الآخرين ولا لمعلّقه؛ دخوله يُرفض بحالة معلنة | `gate-org.spec.ts` «ACC-63» (T2.3) | Chromium بلا رأس (سياق واحد = جهاز مشترك بتبديل المستخدم) + Django | **يمرّ آلياً** — معلّق الكاشير يبقى بعد التعطيل ويُرفع من جلسة المالك ويظهر في الكشف (100.00)، المالك يواصل البيع (200.00)، والكاشير يرى «موقوف»/«لا منشأة بعد» لا خطأً غامضاً | رسالة «سُحب وصولك» المخصّصة (ACC-45) غير مبنية — تُعرض حالة ACC-03 الموقوفة؛ إغلاق وردية المعطَّل إدارياً غير مبني (0005 §١٦) |
| ACC-72 | خمس مبيعات آجلة 100 ثم تحديث واختبار الاستئناف → دين 500 وخمس عمليات | — | — | **غير مُثبت آلياً** — تحديث الويب = إعادة تحميل بعد تسجيل الأثر (0005 §٤٢) والجلسة في الذاكرة فقط فلا استئناف بلا دخول جديد | يُثبت يدوياً على جهاز مثبَّت (PWA) بعد نشر إصدار فعلي |
| ACC-105، ACC-109 (وACC-103) | زبون يشترك بمحلين ثم يلغي قناة أحدهما → يبقى بالثاني وحده بلا كشف عن الآخر؛ إلغاء حملة أو تفضيل بعد الجدولة وقبل الإرسال → يُعاد التحقق قبل المحاولة ولا نشر لاحق | `gate-campaigns.spec.ts` «ACC-105/109» (T2.12) | عبر الـAPI على Django الحقيقي بجلستي حساب (المالك عضو في المنشأتين) — الإذن عبر `POST /api/scenario/marketing` ينوب عن رابط الاشتراك/QR حتى يُبنى | **يمرّ آلياً** | مزوّد الرسائل محاكاة (`sms_*` في مفاتيح الأعطال) |
| ACC-80، ACC-81 | اشتراك منتهٍ منذ 20 يوماً على جهاز بديل → التسجيل والتنزيل الأول يعملان، بيع نقدي وآجل، كشف الحساب وتصديره برابط مخوَّل؛ منذ 35 يوماً → تسجيل جهاز جديد يُرفض بـ`subscription_expired` والجهاز القائم يواصل البيع | `gate-subscription.spec.ts` «ACC-80/81» (T2.17) | Chromium بلا رأس + Django؛ الاشتراك عبر `POST /api/scenario/subscription` | **يمرّ آلياً** | استعادة نسخة على الجهاز البديل بعد الانتهاء غير مختبرة هنا (ACC-73 تثبتها باشتراك نشط)؛ الميزات المتقدمة (التقارير التحليلية، الاستيراد) لا تُوقف خادمياً بعد المهلة — الحملات وحدها (0005 §٦٣) |
| ACC-82 | «فرعان» بحملة مسودة ثم معلّق على جهاز (تجميد) ثم تخفيض إلى «فرع واحد» → الحملة السابقة تبقى مقروءة، حملة جديدة تُمنع بـ`feature_unavailable` معلَناً، والمعلّق يُرفع بلا ضياع | «ACC-82» (T2.17) | نفسه | **يمرّ آلياً** | — |
| ACC-114 | تجميد المصالحة + إعلان صيانة → صفحة الحالة تقول «تعطل جزئي» والمزامنة «متوقفة جزئياً» والبيع «يعمل»، والجهاز يبيع محلياً ثم يرفع بعد العودة و«كل الخدمات تعمل» | `gate-public.spec.ts` «ACC-114» (T2.17) | صفحة `/status` تقرأ `/api/public/status` (مفتاحا `freeze_reconciliation`/`maintenance`) | **يمرّ آلياً** | الاستضافة المستقلة لصفحة الحالة قرار نشر ينتظر المالك (0005 §٦٢)؛ Redis/عامل الإشعارات غير موجودين بعد فلا يُحاكى تعطلهما |
| ACC-60 (وACC-121) | رابط كشف حساب لا وجود له ورابط بصيغة منشأة أخرى → الصفحة نفسها بالحرف (`/link-expired`)، والبرامج 404 بلا تفصيل | «ACC-60» (T2.17) | نفسه | **يمرّ آلياً** | تبديل معرّفات داخل الـAPI (ACC-121) مثبت بوحدات RLS/`test_cross_tenant` لا بالبوابة |
| ACC-83 | طباعة عربية ومختلطة على طرازين | — | عتاد فعلي | **غير مُثبت** — انظر `printers.md` | ينتظر طرازين BLE عند المالك |

## خطوات التجربة اليدوية للمالك (لكل سيناريو آلي)
1. `cd backend && STING_ENV=development STING_FAULTS_ENABLED=1 uv run python manage.py scenario reset` — يطبع الحساب (`+249912447001` / `sting-demo-2026` / PIN `123456`).
2. جهازان (متصفحان أو هاتف وحاسوب): `/login?next=/setup-device` → اختر «بقالة النيل — تجريبي» → انتظر «الجهاز جاهز» → افتح وردية بـ0.00.
3. نفّذ صف التجربة من جدول §١٥.٤ ثم تحقق من الشاشات: كشف العميل (العملاء والذمم → أحمد الطيب)، الوردية والصندوق، المخزون. المفاتيح: `POST /api/scenario/faults {"key":"drop_ack","on":true}` وغيرها.
4. النتيجة المتوقعة = عمود «النتيجة المتوقعة» في §١٥.٤ حرفياً. أي فرق يُسجَّل هنا في «العيوب المفتوحة» مع commit والجهاز.

آخر تشغيل آلي: 2026-09-19 على فرع `phase2/t2.17-gate` — 13/13 سيناريو تمرّ محلياً (Chromium بلا رأس، macOS)؛ CI هو المرجع. الحالة الابتدائية تُصفّر مفاتيح الأعطال أيضاً (`POST /api/scenario/reset`).
