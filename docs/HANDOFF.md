# تسليم الجلسة — من يقرأ هذا يبدأ من هنا لا من الصفر

آخر تحديث: 2026-09-21 · `main` يحوي المراحل ٠–٣ و٣ب كاملة + جولة UX/الهوية (#111؛ 0005 §٩٢–§١١٢: الاسم «فيزانو»، نظام ألوان فيزانو فاتح/داكن، الخطوط المحلية، صفحة هبوط بترويسة عامة موحَّدة وقسم تواصل، تسجيل حساب، مجلدات بناء منفصلة، `market_m3` مرفوع في بيئة التطوير) · **لا مهمة بناء تالية**: ما بقي قرارات المالك (تفعيل M1…M4 من PLT-12، التجربة اليدوية §١٥.٥، G-04 التسعير، المرحلة ٤ Expo/Tauri بإذن كتابي بعد التجربة الميدانية)

هذه الوثيقة مكتوبة لمنفّذ (Claude Code) يبدأ دردشة جديدة. اقرأها كاملة، ثم اقرأ الملفات المذكورة في §1 بالترتيب، ثم نفّذ §5 حرفياً قبل أي سطر كود.

---

## 0 · الرسالة الأولى المقترحة للدردشة الجديدة

انسخ هذا للمنفّذ الجديد (كاملاً — هو كل ما يحتاجه ليبدأ من حيث توقفنا):

> أنت تواصل تنفيذ مشروع Sting Systems في `/Users/macbookairm1/Documents/vezona-business` (remote `Stingcompiler/vezano-business`) من حيث توقفت جلسة سابقة امتلأت ذاكرتها. ابدأ حرفياً هكذا:
> 1. اقرأ `docs/HANDOFF.md` كاملاً (ترتيب القراءة في §1، الحالة في §2، الأوامر والفخاخ في §3، ما ينتظر المالك في §4، خطوات البدء في §5) ثم `docs/decisions/0005-acc-01-02-frame-conflicts-and-identity.md` §١–§٩٠ (القرارات والافتراضات المسجّلة؛ §٤٦ = بوابة T1.43، §٤٧–§٥٢ = ORG-01…10، §٥٣–§٥٥ = REP-01…06، §٥٦–§٥٨ = NOT-01…06، §٥٩–§٦١ = PUR-01…05، §٦٢ = PUB-01…04، §٦٣ = بوابة T2.17، §٦٤–§٦٥ = CUS-01…05، §٦٦–§٧٢ = MP-08…13 وMP-01…07 وMP-14/15، §٧٣–§٧٩ = ORD-01…15، §٨٠ = LINK/GROW phase_locked، §٨١–§٨٦ = PLT-01…12، §٨٧ = بوابة T3.24، §٨٨ = تفسير المرحلة ٣ب، §٨٩ = LINK-01/02، §٩٠ = LINK-03/04/05).
> 2. `git fetch --all` ثم `gh pr list`: أي PR مفتوح لمهمة — بالترتيب: خضرة الفحوص **الأربعة** (backend، packages، e2e، gate) ⇒ `gh pr merge N --merge --delete-branch` — لحظة التسليم المفتوحة هي **#104 → #105 → #106** بهذا الترتيب (السلسلة كلها تحت `phase3/`؛ سقوط `web` بـ`ERR_CONNECTION_REFUSED` = عطل خادم التطوير في CI فأعد تشغيل الفاشل `gh run rerun <id> --failed` لا تعدّل شيئاً) (الإذن قائم منذ T1.9 لكل PR أخضر)؛ أحمر ⇒ أصلح على فرعه أولاً. PRs #1–#82 مدمجة (المرحلتان ١ و٢).
> 3. في worktree جديد: `pnpm install` ثم `cd backend && uv sync && STING_ENV=development STING_FAULTS_ENABLED=1 uv run python manage.py migrate` ثم `cd apps/web && pnpm exec playwright install chromium`؛ ولقواعد الاختبار مرة واحدة: `createdb sting_t115 && createdb sting_gate_test` (حارس الإنتاج يشترط وسم `_test`).
> 4. تحقق بأربعة فحوص، كلٌّ وحده: `pnpm check` (Vitest 348 + 1 todo) · `cd backend && DATABASE_URL=postgresql:///sting_t115 STING_ENV=test STING_FAULTS_ENABLED=1 uv run pytest -p no:warnings` (426 — يشمل `market` و`stingops` منذ T3.20) · `cd apps/web && PLAYWRIGHT_PORT=3011 pnpm exec playwright test --workers=3` (1506 = 502 × 3؛ أكثر من ساعة — CI هو المرجع، ومحلياً شغّل مواصفة المهمة وحدها؛ `PLAYWRIGHT_PORT` لأن 3000 مشغول بمشروع آخر؛ لا تعدّل الشيفرة أثناءه) · `cd apps/web && pnpm e2e:gate` (14 سيناريو: §١٥.٤ بجهازين + سيناريو السوق §١٥.٥ بثلاث منشآت، على Django حقيقي — بعد أي تعديل خادمي اقتل خادم 8100 القديم أولاً — يشغّل Django على 8100 وNext على 3100 بنفسه؛ المنفذ 8000 مشغول على هذا الجهاز بخادم قديم لا تقتله). فشلٌ يُبلَّغ ولا يُرقَّع؛ اختبار يسقط في التشغيل الكامل ويمرّ وحده = حِمل لا عيب.
> 5. **المرحلتان ٣ و٣ب مكتملتان آلياً** (T3.1–T3.27؛ PRs #83–#109 مدمجة كلها — لا PR مفتوح لحظة التسليم). كل شاشات الويب المرسومة مبنية بكل حالاتها المرسومة؛ M3 (LINK-01…05) وM4 (GROW-01…03) تعمل خلف علمي `market_m3`/`market_m4` من PLT-12 وتبقى `phase_locked` بنصّها حتى يرفعهما المشغّل؛ بوابات MK-1…4 كلها تمرّ آلياً (`e2e-gate` 15/15). **لا مهمة بناء تالية في PLAN** — ما بقي ينتظر المالك (§4): قرار تفعيل M1/M2/M3/M4 (0005 §٨٧/§٩١)، التجربة اليدوية §١٥.٥ على جهازين، تسعير العرض الممول G-04، الافتراضات المعلَّقة، والمرحلة ٤ (Expo/Tauri) بإذن كتابي بعد التجربة الميدانية (PLAN §١٥.١ مرحلة د) — لا تبدأها بلا نصّه. إن أعطى المالك مهمة جديدة: فرع من `main` والحلقة نفسها لكل شاشة: `frameTextsAll` ← البناء من `@sting/ui-web` فقط بجذر `data-screen`/`data-state` ← Playwright بـ`expectFrame`/`fromFrame` على 390/834/1440 ← commit ← `gh pr create --base main` ← دمج بعد خضرة CI؛ ولكل معيار §١٨ ذي أثر مرئي سيناريو في `apps/web/e2e-gate` وصفّ في الأدلة. لا تُفعَّل ميزة سوق قبل بوابتها ولو بُنيت شاشتها. ما ينتظر المالك في §4 لا تفعله بلا مدخلاته.
> 6. عند أي غموض أو تعارض بين الإطارات: سجّله ملحقاً جديداً في `docs/decisions/0005` بافتراض معلن واستمر؛ لا تخترع شاشة غير مرسومة.
> 7. قبل أن تمتلئ ذاكرتك: نفّذ `/handoff` (مهارة في `.claude/skills/handoff`) لتحديث هذه الوثيقة والذاكرة وطباعة الرسالة الأولى للدردشة التالية.
>
> القواعد غير القابلة للتفاوض: نصوص حرفية من الإطار بما فيها التشكيل؛ 17 حالة فقط وما هو مرسوم للشاشة في `states-matrix.csv`؛ أرقام لاتينية في `.sting-mono` ولا حرف عربي داخله؛ RTL بخصائص منطقية؛ لمس ≥44px؛ لا تقريب في الواجهة؛ الجلسة في الذاكرة فقط (لا `localStorage`، و`page.goto` بعد الدخول يعيد إلى `/login` — تنقّل بالروابط)؛ كل كتابة مرجعية خادمية عبر `log_reference()`؛ إسقاطات PUSH عبر `sync.appliers`؛ مُسقِط Dexie لا ينتظر مساعداً `async` داخل `storage.transaction`؛ تقرير بعد كل تسليم: ما بُني/اختُبر/بقي، بلا تقديرات زمنية.

---

## 1 · ما يحكم ماذا (اقرأ بالترتيب)

| # | الملف | لماذا |
|---|---|---|
| 1 | `CLAUDE_CODE_PROMPT.md` (الجذر) — **القسم ٣ (القواعد غير القابلة للتفاوض) والقسم ٤ (بوابة الخروج)** | الأمر الحاكم. لا يُتجاوَز بند فيه |
| 2 | `docs/PLAN.md` — **§٣ المرحلة ١** | جدول المهام T1.1–T1.43 بمراجعها ومعايير إنجازها |
| 3 | `docs/decisions/0002-open-questions-from-reading.md` | **س٤ ما زالت مفتوحة** (انظر §4 أدناه) |
| 3b | `docs/decisions/0005-acc-01-02-frame-conflicts-and-identity.md` | قاعدة تطبيق «بطاقات الحالة» D26، تعارضات D2/D8/D26، نموذج الهوية (Account بعدة عضويات)، وملاحق كل مهمة T1.x/T2.x/T3.x (§٥–§٩٠) بافتراضاتها |
| 4 | `docs/decisions/0004-ui-rules-follow-design-system.md` | حلقة التركيز، ارتفاعات اللمس، أحجام النص — محسومة باتباع نظام التصميم |
| 5 | `docs/ARCHITECTURE.md` | البنية المشتقة من v21 (§8 المزامنة، §9 المصادقة، §13 النسخ) |
| 6 | `design_handoff_sting_systems/README.md` + `handoff/states-matrix.csv` | 164 شاشة / 754 زوج شاشة×حالة مرسوم. **الإطار مصدر الحقيقة الوحيد** |
| 7 | `design_handoff_sting_systems/Sting-Systems-v21-Complete.md` | مواصفة الأعمال؛ يُرجَع إليها بالقسم (§) المذكور في PLAN.md |

لا تقرأ المستودع كله؛ اقرأ ما تحتاجه لمهمتك من §3.

---

## 2 · حالة المستودع لحظة التسليم

### الفروع والطلبات

`main` يحوي `.gitignore` الأساسي فقط. كل العمل في **سلسلة طلبات مكدّسة** كل واحد مبني على سابقه:

```
#1  docs/plan-and-design-handoff          → main
#2  phase0/t0.1-repo-skeleton             → main
#3  phase0/t0.2-design-tokens             → #2
#4  phase0/t0.3-domain-money              → #3
#5  phase0/t0.4-domain-qty-effects        → #4
#6  phase0/t0.5-backend-core-isolation    → #5
#7  phase0/t0.6-auth-devices-pin          → #6
#8  phase0/t0.7-sync-push                 → #7
#9  phase0/t0.8-sync-pull-snapshots       → #8
#10 phase0/t0.9-contracts                 → #9
#11 phase0/t0.10-platform-contracts       → #10
#12 phase0/t0.11-dexie-adapter            → #11
#13 phase0/t0.12-sync-core-queue          → #12
#14 phase0/t0.13-sync-core-balance        → #13
#15 phase0/t0.14-ui-web-foundation        → #14
#16 phase0/t0.15-ui-web-input-dialog      → #15
#17 phase0/t0.16-ui-web-tables-lists      → #16
#18 phase0/t0.17-ui-web-financial         → #17
#19 phase0/t0.18-ui-web-market-campaign   → #18
#20 phase0/t0.19-web-bootstrap            → #19
#21 phase0/t0.20-scenario-seed            → #20
#22–#32 phase1/t1.1 … t1.11              → main (كل واحد دُمج بعد خضرة CI)
#33 phase1/t1.12-shift-03-04              → main (مدمج)
#34 phase1/t1.13-shift-05                 → main (مدمج)
#35 phase1/t1.14-pos-01-02                → main (مدمج)
#36 phase1/t1.15-pos-03-04                → main (مدمج)
#37 phase1/t1.16-pos-05-sale              → main (مدمج)
#38 phase1/t1.17-pos-06-07                → main (مدمج)
#39 phase1/t1.18-pos-08-11                → main (مدمج)
#40 phase1/t1.19-pos-09-12                → main (مدمج)
#41 phase1/t1.20-pos-10                   → main (مدمج)
#42 phase1/t1.21-unified-scenario         → main (مدمج)
#43 phase1/t1.22-pty-01-02                → main (مدمج)
#44 phase1/t1.23-pty-03-04                → main (مدمج)
#45 phase1/t1.24-pty-05                   → main (مدمج)
#46 phase1/t1.25-pty-06                   → main (مدمج)
#47 phase1/t1.26-pty-07-09                → main (مدمج)
#48 phase1/t1.27-pty-08                   → main (مدمج)
#49 phase1/t1.28-inv-01-02                → main (مدمج)
#50 phase1/t1.29-inv-03-04                → main (مدمج)
#51 phase1/t1.30-inv-05-06                → main (مدمج)
#52 phase1/t1.31-inv-07                   → main (مدمج)
#53 phase1/t1.32-inv-08-09                → main (مفتوح عند التسليم — **ادمجه أولاً بعد خضرة CI**)
```

**كل ما سبق مدمج في `main` عدا #53.** فرع البداية للمهمة التالية هو `main` بعد دمج #53؛ الفروع القديمة محذوفة. الدمج من المنفّذ مأذون به بعد خضرة CI (منذ T1.9).

CI أخضر على كل الطلبات #2–#21 (وظائف `python`/`node`، و`e2e` من #20).

**الدمج مسؤولية المالك** (صنّف الأذونات منع `gh pr merge` من المنفّذ). الترتيب: #1 ثم #2 ثم #3 … #21. بعد كل دمج يعيد GitHub توجيه قاعدة الطلب التالي إلى `main` تلقائياً. إن دُمج الكل: فرع البداية للمرحلة ١ هو `main`؛ وإلا فهو `phase0/t0.20-scenario-seed`.

### ما اكتمل (المرحلة ٠ — 20 مهمة)

| الطبقة | المحتوى | الاختبارات |
|---|---|---|
| `packages/domain` | نقود BigInt (minor) + تقريب half-away، كميات milli + معاملات وحدات، دفتر، آثار، وردية. **مرايا Python** في `backend/core/{money,quantities,ledger,effects,shift}.py` ومتجهات JSON مشتركة في `packages/domain/vectors/` | Vitest + pytest على نفس المتجهات |
| `backend/core` | Tenant/Branch/Device/User/Role/UserBranchAccess/TenantSettings/Session/PinVerifier؛ عزل ثلاثي (RLS بدور `sting_app` + `tenant_context()` fail-closed + FKs مركبة)؛ JWT (تدوير + قائمة سوداء)؛ PIN؛ تسجيل الأجهزة | isolation 21، auth 14 |
| `backend/sync` | PUSH بقواعد القبول العشر (§8)، عدّاد المستأجر (قفل قبل فحص التكرار)، `sync_log`؛ PULL في REPEATABLE READ بمؤشرات (scope, group)؛ AccessManifest؛ Snapshots؛ عدم تطابق epoch → 409 | push 23، pull 14 |
| `packages/contracts` | `openapi.json` + `openapi-typescript` + `openapi-fetch` + اختبار انحراف | drift test |
| `packages/platform` | StoragePort ذرّي، MemoryStorage، DexieStorage، Web Locks، طلب التخزين المستديم، اختبارات عقد مشتركة | contract tests |
| `packages/sync-core` | saveOperation، ترقيم `INV-KRT-A2-26-000001`، إعادة محاولة بتذبذب، pushOnce/applyResults، معادلة الرصيد ①②③، مرشح النسخة وتفعيلها بـ`local_frontier`، applyPullPage، adoptEpoch، pruneVerdict | ✓ |
| `packages/design` | مولّد `tokens.json` → `theme.css` (`@theme{}` + نسخة `:root{}`)، `tokens.ts`، `states.ts` (17 حالة بنصوصها الحرفية)، `fonts.css` + `fonts.faces.css` اختياري | ✓ |
| `packages/ui-web` | **29 مكوّناً** كاملة (Frame, Nav, OrgSwitcher, Button, Status, Field×5, Notice, Dialog, Sheet, Panel, Table, Filter, Pick, TimeList, Upload, DocPreview, Money/MoneyInput/Settlement, QtyUnit, Sync, LedgerLine, Cart, Receive, Print, Phase, Listing/Compare, Quote, OrderTimeline, Audience, Campaign) + Storybook 10 + addon-a11y؛ كل مكوّن له قصة واختبار `expectNoA11yViolations` + `expectNoArabicInMono` | 264 Vitest |
| `apps/web` | Next.js 15.5 App Router، React 19، TanStack Query (بيانات الخادم فقط)، Context للغة/الجلسة/الفرع، `/dev/probe`، Playwright بثلاثة مقاسات 390/834/1440، `e2e/frame-match.ts` | 15 Playwright |
| `tools/frame-text` | مستخرج النصوص الحرفية من الإطارات؛ يثبت اختباره أن **754/754** زوجاً قابل للاستخراج | ✓ |
| `tools/boundaries` | اختبارات سلبية لقواعد dependency-cruiser | ✓ |
| السيناريو | `manage.py scenario reset|wipe`؛ `/api/scenario/{reset,faults}` فقط حين `STING_FAULTS_ENABLED=1` و`STING_ENV∈{development,test,ci}`؛ مفاتيح الأعطال drop_ack / freeze_reconciliation / network_cut / printer_fail | scenario 7 |

المجاميع بعد T3.27: **pytest 429 · Vitest 348 (+1 todo) · Playwright 1506 (502 × 3 مقاسات — من تشغيل CI لـ#109: 1499 ناجحاً + 7 متذبذبة نجحت بالإعادة؛ محلياً بـ`--workers=3`) · gate 15/15** (البوابة 15/15 محلياً على Django حقيقي — 13 من المرحلتين ١ و٢ + سيناريو السوق §١٥.٥ + MK-3 — `docs/evidence/README.md`).

### ما اكتمل من المرحلة ١

| المهمة | المسارات | الخادم | الاختبار |
|---|---|---|---|
| T1.1 ACC-01 (3) + ACC-02 (7) | `/welcome`، `/login` (خطوات login/verify/manual) | `Account` + `auth/account/login` + `auth/verify/*` + `api/health` + عطل `verify_send_fail` | `apps/web/e2e/acc.spec.ts` بمحاكاة الشبكة؛ `e2e/frame-provenance.ts` يثبت أن كل نص متوقَّع في الإطار |
| T1.2 ACC-03 (6) + ACC-04 (5) | `/select-org`، `/create-org` | `Unit`/`PaymentMethod`/`TenantCreation`، وصفة البقالة، `account/memberships|select`، `tenants` (متكرّر الأثر) | `e2e/acc-org.spec.ts` |
| T1.3 ACC-05 (6) | `/setup-device` | `BootstrapImage/Page` (نسخة مادية بصفحات مجمّدة)، `devices/register|renew`، `bootstrap/*` | `sync-core/bootstrap.ts`؛ `e2e/acc-setup.spec.ts` (IndexedDB حقيقي) |
| T1.4 ACC-06 (5) + ACC-07 (4) | `/invite/[token]`، `/lock` | `Invitation`، `invites/*`، `devices/verifiers` | `sync-core/pin.ts` (PBKDF2 عبر WebCrypto، قفل 5/15)؛ `e2e/acc-invite-lock.spec.ts` |
| T1.5 ACC-08 (5) + ACC-09 (6) | `/session-expired`، `/account/sessions` | `Session.reported_pending/revoke_after_upload`، `account/sessions[/{id}/revoke]`، `pending_after` في PUSH | حارس 401 في Providers؛ `e2e/acc-session.spec.ts` |
| T1.6 ACC-10 (6) | `/onboarding` | `tenants/onboarding` (GET/PATCH؛ الشعار ≤2MB) | `e2e/acc-onboarding.spec.ts` |
| T1.7 HOME-01 (6) + HOME-02 (5) + HOME-03 (5) | `/` (مالك/موظف)، `/search` | `core/home.py` سجلّ مزوّدين (`HOME_PROVIDERS`/`SEARCH_PROVIDERS`)، `home|search|notices` | `platform.listProjections`؛ `e2e/home.spec.ts` |
| T1.8 CAT-01 (5) + CAT-06 (4) | `/catalog`، `/catalog/groups` | تطبيق `catalog` (Item/ItemGroup/ItemUnit/ItemAlias)، `sync/reference.py` (مرجعيات خادمية في sync_log/PULL/النسخة) | `domain/search.ts` + مرآة Python بمتجهات؛ `sync-core/catalog-local.ts`؛ `e2e/catalog.spec.ts` |
| T1.17 POS-06 (5) + POS-07 (7) | `/pos/pay/credit`، `/pos/pay/mixed` | `sales.CreditOverride` + نوع PUSH `credit_override` (يعتمد على عملية البيع؛ السبب والحدّ والرصيد بعده إلزامية) + مُطبِّقه | `sync-core/sale-local.ts`: `readPartyPendingCredit` (معلّق هذا الجهاز للطرف)، `recordCreditOverride`، `bankReferenceUsed`؛ `features/pos/{use-sale.ts,pay-methods.tsx,credit-client.tsx,mixed-client.tsx}`؛ `e2e/pos-credit-mixed.spec.ts` (يثبت أن SHIFT-02 يرى +40 من الحفظ نفسه) |
| T1.18 POS-08 (5) + POS-11 (4) | `/pos/receipt/{id}` (`last` = آخر بيع)، `/pos/receipt/{id}/problem` | — (لا تغيير خادمي؛ `printer_fail` من `/api/scenario/faults`) | `lib/sync.ts`: `sync.last_push` (`readLastPush`)؛ `features/pos/{receipt-client.tsx,problem-client.tsx}` (الحالة من حالة العملية: local/pending/synced/quarantined + الشبكة + آخر رفع؛ الإيصال بـC-PRINT بوسم «محفوظ على الجهاز» حتى التأكيد و«نسخة» بعد الأولى)؛ شاشات الدفع تنتهي بـ«طباعة الإيصال»/«بيع جديد»؛ `e2e/pos-receipt.spec.ts` |
| T1.19 POS-09 (7) + POS-12 (4) | `/pos/invoices`، `/pos/invoices/{id}`، `/pos/duplicates` | `sales.SaleReversal` (مستند إلغاء مستقل — مرجع `log_reference` بنطاق branch/sales)، `DuplicateDecision`، `DuplicateReport`؛ `sales/duplicates.py` (القائمة بنطاق المشاهد، `date_suspect` ACC-77، كشف الأزواج: جهازان + نفس السطور خلال 60 ث، القرار عكس/حقيقيان)؛ `sales/views.py`: `GET /api/sales`، `GET /api/sales/{id}`، `GET /api/sales/duplicates`، `POST /api/sales/duplicates/decide` (403 `manager_required`)، `POST /api/sales/{id}/duplicate-report`؛ مزوّدا الدرج والذمّة يخصمان الملغاة | `features/pos/{invoices-client,invoice-detail-client,duplicates-client}.tsx` (المحلي فوراً + الخادمي موسوماً، `sales.list_cache` للحالة stale)؛ `tools/frame-text` يتبع مساعدات القوائم (`inv`/`st`)؛ `e2e/pos-invoices.spec.ts` |
| T1.20 POS-10 (6) | `/pos/invoices/{id}/return` | `sales.SaleReturn`/`SaleReturnLine` + `inventory.QuarantineMovement` (الحجر/الهالك — ACC-10)؛ نوع PUSH `sale_return` (يشتق المجموع والحركات: صالح → مخزون، تالف → حجر؛ خصم ذمّة يحتاج طرفاً) ومُطبِّقاته (`exceeds_original` عند تجاوز السقف التراكمي — يُوسم لا يُرفض، ACC-11)؛ مزوّدو الدرج (`cash_refunds`)، المتأخر (`refund`)، والذمّة | `sync-core/return-local.ts` (`saveReturnLocally`، `readReturnedQty`، `readReturnCashRows`، `saleFromServer`؛ رقم `RET-…` بعدّاد `return_seq`)؛ `features/pos/return-client.tsx` (حدود الردّ بحسب الدور G-09 مؤقتاً)؛ SHIFT-02/04 وPOS-01 يضمّون `readReturnCashRows`؛ C-FIELD radio بطاقة كاملة الحافة؛ `e2e/pos-return.spec.ts` |
| T1.21 السيناريو الموحَّد | — | `party_payload` يحمل `balance_as_of` (وقت تغطية الرصيد الخادمي) | `sync-core/sale-local.ts::readPartyPendingCredit` يركّب ما بعد `balance_as_of` ولو أُكِّد؛ `e2e/unified-scenario.spec.ts` (١٠٠ = ٤٠ + ٦٠ في POS-07/SHIFT-02/POS-06/POS-09 قبل الرفع وبعده؛ POS-06 نائب PTY-05) |
| T1.22 PTY-01 (6) + PTY-02 (4) | `/parties`، `/parties/suppliers` | `Party.aliases`/`aliases_normalized` (البحث بالبادئة)؛ `GET /api/parties/list?kind=customers|suppliers` (العملاء لمن يرى المال — 403 `finance_required`؛ الموردون أسماءً للجميع و`can_see_balances`)؛ `SUPPLIER_OWED_PROVIDERS` فارغ حتى INV | `sync-core/parties-list.ts` (`storeParties` بوقت التغطية، `readPendingCreditByParty`، `parties.matched_at`)؛ `features/parties/{customers-client,suppliers-client}.tsx` + `parties.css`؛ `tools/frame-text` يتبع `.map(…)` بعد مصفوفة القائمة؛ `e2e/parties.spec.ts` |
| T1.23 PTY-03 (6) + PTY-04 (4) | `/parties/{id}`، `/parties/{id}/opening` | `Party.note`؛ `OpeningBalance` (جهة/مبلغ/سبب/مرجع/تاريخ اختياري = «قبل النظام»؛ مرة لكل صفة وقبل أول حركة؛ مرجع `parties.OpeningBalance` مؤسسي)؛ `GET/PATCH /api/parties/{id}` (403 `manager_required`؛ `potential_duplicates`، `has_movements`)، `POST /api/parties/{id}/distinct`، `POST /api/parties/{id}/opening-balance` (403 `owner_required`)؛ `MOVEMENT_PROVIDERS` (البيع) | `features/parties/{party-card-client,opening-client}.tsx` (رصيدان منفصلان، تعديل، تكرار محتمل «منفصلان»، الافتتاحي بمعاينة الأثر)؛ `e2e/parties-card.spec.ts` |
| T1.24 PTY-05 (7) | `/parties/{id}/statement` | `parties.services.StatementLine` + `STATEMENT_LINE_PROVIDERS` (البيع الآجل/المختلط مديناً، المرتجع خصماً دائناً، النقدي معلوماتياً؛ الافتتاحي أولاً) و`statement_payload` (رصيد جارٍ من كل الحركات، `hidden_other_branch` بنطاق الفرع — ACC-46، `last_payment_at`/`oldest_unpaid_at`)؛ `GET /api/parties/{id}/statement?range=30|all` (403 `finance_required`) | `features/parties/statement-client.tsx` (المعلّق من هذا الجهاز بوسمه داخل الرصيد، التركيب المعلن، نسخة `parties.statement.<id>` لـoffline/stale، 7 حالات)؛ `e2e/parties-statement.spec.ts` |
| T1.25 PTY-06 (6) | `/parties/{id}/payment` | `PaymentReceipt` + نوع PUSH `payment_receipt` (سداد/ردّ، نقد/تحويل، السبب للردّ، المرجع للتحويل بقيد تفرد — ACC-15) ومُطبِّقه؛ الذمّة عبر `BALANCE_PROVIDERS` (النقد فوراً، التحويل بعد `POST /api/parties/receipts/{id}/match` — ACC-133)؛ الدرج (`cash_debt_receipts`/`cash_refunds`) والمتأخر والكشف | `sync-core/receipt-local.ts` (`saveReceiptLocally` برقم `REC-…`، `readReceiptCashRows`، `readPendingReceiptsByParty`)؛ الرصيد المركّب يخصم السداد النقدي المحلي (ACC-03)؛ `features/parties/payment-client.tsx`؛ PTY-05 يعرض السندات المعلّقة و«تسجيل سداد» مفعَّل؛ SHIFT-02/04 وPOS-01 يضمّون سطور السندات؛ `e2e/parties-payment.spec.ts` |
| T1.26 PTY-07 (5) + PTY-09 (4) | `/parties/{id}/merge?target=`، `/parties/receipts/{id}/correct` | `Party.merged_into` + `PartyMerge` (خريطة هوية ACC-78: `identity_ids` في مزوّدي الرصيد/آخر بيع/الكشف؛ سطر الكشف يحمل `source_party`)؛ `GET/POST /api/parties/{id}/merge` (معاينة؛ `confirm:"MERGE"`؛ مالك؛ لا حلقات) و`POST /api/parties/merges/{id}/undo` (بالعدد لا بالزمن)؛ `ReceiptCorrection` (وسيلة/عكس/مبلغ/تاريخ؛ الأصل ثابت؛ `effective_receipt`؛ الفترة المقفلة = قبل أول الشهر السابق) و`GET/POST /api/parties/receipts/{id}/correct`؛ المدموج يختفي من القوائم | `readLocalParties` يُسقط `merged_into`؛ `features/parties/merge-client.tsx` (تأكيد مزدوج بحوار خطر بكلمة «دمج») و`correction-client.tsx` (الخيار الرابع «تصحيح تاريخ الأعمال»)؛ PTY-03 «دمج بتأكيد مزدوج» مفعَّل؛ PTY-05 يعرض «· المصدر — …» وزرّ «تصحيح حركة» للمالك؛ `e2e/parties-merge.spec.ts` |
| T1.27 PTY-08 (5) | `/parties/{id}/statement/share?range=` | `StatementExport` (لقطة الكشف بحقوله ومداه ووقته وعدد صفحاته، رمز الرابط، `opened_at`/`open_count`)؛ `POST /api/parties/{id}/statement/export` (مالك؛ `too_long` فوق 500 سطر)؛ `GET /api/parties/statement-exports/{id}` (الحالة بصدق)؛ `GET /api/parties/exports/{token}` مستند HTML بلا جلسة يسجّل «تم الاطلاع»؛ `statement_payload.tenant_name` | `features/parties/share-client.tsx` (الحقول المختارة، معاينة المستند تُطبع وحدها بترويسة الوقت، «جارٍ التوليد» بالصفحات المتوقَّعة، «جاهز»/«لا وعد تسليم» بحالة «لم يُفتح بعد»→«تم الاطلاع»، «فشل التوليد» ببديل «مدى أقصر»، قالب تذكير يدوي بلا إرسال)؛ PTY-05 «طباعة وتصدير» مفعَّل؛ `e2e/parties-share.spec.ts` |
| T1.28 INV-01 (7) + INV-02 (5) | `/inventory`، `/inventory/items/{id}?branch=&range=` | `Item.alert_threshold_milli` (PATCH)؛ `inventory/services.py::stock_rows` (كل صنف له حركة: الرصيد بالوحدة الأساسية، وحدة الشراء بمعاملها، الوسم، الحجر، آخر حركة) و`item_movements` (المصدر والمستند عبر `MOVEMENT_SOURCE_RESOLVERS` من `sales`، الرصيد بعد كل حركة)؛ `GET /api/inventory/balances?branch_id=` و`GET /api/inventory/items/{id}/movements?branch_id=&range=` (المالك أي فرع؛ غيره فرعه) | `sync-core/inventory-local.ts` (`readPendingStockMovements`/`pendingByItem`/لقطات `inventory.stock_cache.*`)؛ `features/inventory/stock-client.tsx` (7 حالات: المواقع واحداً واحداً، «كل المواقع» لا يُعرض جزئياً، المعلّق موسوم، بلا اتصال من أرصدة الجهاز) و`movements-client.tsx` (5 حالات: المعلّق أولاً بلا «رصيد بعدها»، «منه N من حركات لم تُرفع بعد»)؛ `e2e/inventory.spec.ts` |
| T1.29 INV-03 (4) + INV-04 (5) | `/inventory/openings`، `/inventory/receive` | `GoodsReceipt`/`GoodsReceiptLine` + نوع PUSH `stock_receipt` (المورد والمرجع والكمية مطلوبة؛ التكلفة اختيارية؛ الوحدة بلا معامل تُرفض؛ الحركات `receive` مشتقة من السطور) ومُطبِّقاه؛ `StockOpening`/`StockOpeningLine` (مستند واحد؛ المالك يعتمد فوراً وغيره يُرسل؛ مرة واحدة لكل صنف وقبل أول حركة) و`POST/GET /api/inventory/openings` و`POST /api/inventory/openings/{id}/approve`؛ مصادر INV-02 للاستلام والافتتاحية | `sync-core/goods-receipt-local.ts` (`saveGoodsReceiptLocally` برقم `RCV-…`، يحرّك الرصيد المحلي ويُنشئه)؛ `features/inventory/receive-client.tsx` (5 حالات) و`openings-client.tsx` (4 حالات: مراجعة بتحويل صريح ولا تقدير؛ «أُرسلت للاعتماد»؛ اعتماد المُرسل)؛ `units.ts`؛ INV-01 يربط الشاشتين؛ `e2e/inventory-docs.spec.ts` |
| T1.30 INV-05 (6) + INV-06 (4) | `/inventory/count`، `/inventory/count/{id}/review` | نوع PUSH `count_session` (`CountSession` + `CountLine` بلقطة الجهاز — بلا حركات) ومُطبِّقاه؛ `StockMovement.note`؛ `session_variances` (الدفتري الآن مقابل المعدود؛ `moved_since_count` — ACC-07؛ الأثر بسعر البيع) و`adjust_session` (سبب لكل فرق، «تسوية جرد» عامّ مرفوض، `StockAdjustment` `TS-NNNN` وحركات `count` بالسبب)؛ `GET /api/inventory/count-sessions`، `GET/POST /api/inventory/count-sessions/{id}` (صلاحية مالية) | `sync-core/count-local.ts` (جلسة مفتوحة تُحفظ بعد كل صنف وتُستأنف؛ `closeCountSession` برقم `CNT-…`)؛ `count-client.tsx` (6 حالات؛ المتوقَّع محجوب حتى يُدخل المعدود — القاعدة 9) و`review-client.tsx` (4 حالات)؛ INV-01 يربط الجرد؛ `e2e/inventory-count.spec.ts` |
| T1.31 INV-07 (4) | `/inventory/items/{id}/damage` | `DamageRecord` (`DMG-NNNN`؛ حجر = حركة `quarantine` + `QuarantineMovement`؛ هالك = حركة `write_off`؛ السبب على الحركة)؛ `record_damage` (الحدّ من الرصيد `exceeds_balance`؛ الحدّ المالي `WRITE_OFF_CAP_MINOR` لغير المالك — مؤقت حتى ORG-02)؛ `POST /api/inventory/items/{id}/damage` | `features/inventory/damage-client.tsx` (4 حالات؛ «المتاح للبيع بعد التسجيل» سالباً بلونه)؛ INV-01 يعرض «حجر — قابل للمراجعة N» تحت الرصيد؛ INV-02 زرّ «تسجيل التالف»؛ `e2e/inventory-damage.spec.ts` |
| T1.32 INV-08 (5) + INV-09 (6) | `/inventory/transfers?id=`، `/inventory/transfers/new?id=` | نوع PUSH `stock_transfer` (`StockTransfer` + `StockTransferLine` + حركات `transfer_out` من المصدر) ومُطبِّقاه؛ `transfer_payload` (في الطريق بكميته وقيمته، `late` بعد 72 ساعة)؛ `visible_transfers` (الفرعان والمالك فقط)؛ `cancel_transfer` (يعيد المتبقّي بحركة `transfer_in`)؛ `in_transit_out` في صفوف INV-01؛ `GET /api/inventory/transfers`، `GET .../{id}`، `POST .../{id}/cancel` | `sync-core/transfer-local.ts` (`saveTransferLocally` برقم `TRF-…`؛ يخصم رصيد المصدر محلياً)؛ `transfer-card.tsx` (بطاقة 14-D9 بدفتريه وحساب الطريق)؛ `transfers-client.tsx` (5 حالات) و`transfer-new-client.tsx` (6 حالات؛ التحقق من المتاح محلياً)؛ INV-01 «في الطريق N» وزرّ «التحويلات»؛ `e2e/inventory-transfers.spec.ts` |
| T1.33 INV-10 (5) | `/inventory/transfers/{id}/receive` | نوع PUSH `transfer_receipt` (`TransferReceipt` + `TransferReceiptLine` + حركات `transfer_in` بمقدار المستلم تشير إلى الإشعار؛ الفرق يوجب `reason`) ومُطبِّقاه (`apply_transfer_receipt` — إشعار ثانٍ لتحويل مستلَم/ملغى يُخزَّن `duplicate=True` بلا أثر؛ `_settle_transfer_status` → `received`/`partially_received`)؛ `receipt_transfer_sources` لعرض «تحويل وارد · من {الفرع}» في INV-02؛ `transfer_payload` يضيف `receipt_count`/`receipt_reason` | `sync-core/transfer-receipt-local.ts` (`saveTransferReceiptLocally` برقم `TRR-…`؛ يزيد رصيد الوجهة بالمستلم ويحدّث إسقاط التحويل)؛ `transfer-receive-client.tsx` (5 حالات: ready/partial/validation_error/conflict/success؛ عدّاد −/+؛ «الثلاثة الباقية» كلمةً)؛ `transfers-client.tsx` يفعّل «تسجيل استلام {الفرع}» للمستقبل والمالك؛ `e2e/inventory-transfer-receipt.spec.ts` |
| T1.34 SYS-01 (6) + SYS-02 (5) | `/sync`، `/sync/operations/{id}` | `GET /api/sync/status` (وقت الخادم، الجيل، الرقم الأعلى، محجور/متعارض هذا الجهاز غير المراجَع، آخر قبول — «هناك شبكة» ≠ «نجح الوصول») | `sync-core/sync-log.ts` (سجل `sync.attempts:<op>` يكتبه `saveOperation`/`pushOnce` على المعاملة؛ `sync.last_ok`؛ `sync.halted`؛ `describeOperation` بلغة المحل؛ `requeueOperation`؛ `renumberSale`)؛ `lib/sync.ts` (`pushPending(…, {manual})` يقف بعد 3 أعطال عابرة، `checkReach`, `reconcileNow`)؛ `features/sys/sync-center-client.tsx` (6 حالات) و`operation-client.tsx` (5 حالات) و`sys-format.tsx`؛ عنصر «المزامنة» في `AppNav`؛ `e2e/sys-sync.spec.ts` |
| T1.35 SYS-03 (4) | `/sync/review?id=`، `/sync/review?op=` | `sync/review.py`: `GET /api/sync/quarantine` (المالك: كل ما ينتظر قراراً بالأثر المالي؛ غيره: محجور جهازه)، `GET .../{id}` (النسختان + الأثر على الرصيد + `acceptable`)، `POST .../{id}/decide` (`accept`/`reject` بسبب؛ `owner_required` 403؛ القبول لسداد مؤكد = `correct_receipt` مبلغ/طريقة؛ غيره `manual_merge_required`؛ `already_decided`)؛ حقول `decision_reason`/`decided_by_name` (هجرة sync 0005) | `features/sys/review-client.tsx` (ready/conflict/permission_denied/success)؛ SYS-02 «مراجعة المالك» يفتح المراجعة بمعرّف العملية؛ `e2e/sys-review.spec.ts` |
| T1.36 SYS-04 (3) + SYS-11 (4) | `/sync/storage?blocked=`، `/support` | `SupportReport` + `POST /api/support/reports` (المالك فقط؛ مفاتيح مسموحة وفحص عميق يرفض أسماء/مبالغ/رموز؛ مرجع `SUP-yymmdd-NNNN`؛ هجرة sync 0006) | `lib/diagnostics.ts` (`storageSnapshot`/`storageLow`/`errorLog`/`usageBreakdown`/`safeCleanup`/`recordPrintFailure`/`recordWriteFailure`/`buildSupportPayload`)؛ `storage-client.tsx` (ready/validation_error/server_error)؛ `support-client.tsx` (ready/empty/permission_denied/success)؛ `use-sale` يمنع الحفظ دون الحدّ الآمن ويحوّل إلى SYS-04؛ POS-08 يسجّل فشل الطباعة؛ `AppNav` «مساحة الجهاز»/«الدعم»؛ `e2e/sys-diagnostics.spec.ts` (يزوّر `navigator.storage.estimate` بـ`addInitScript`) |
| T1.37 SYS-05 (5) + SYS-06 (6) | `/sync/backup`، `/sync/restore` | `BackupCopy` + `POST /api/support/backups` (الغلاف المشفّر كما هو؛ 15 ميغابايت؛ هجرة sync 0007) | `sync-core/backup.ts` (`collectBackup` بلا أسرار، `sealBackup`/`openBackup` PBKDF2→AES-GCM، `inspectBackup` يرفض تالف/منشأة أخرى/إصدار أحدث قبل فكّ التشفير، `previewRestore` الفرق لا المحتوى، `applyRestore` دمج بالهويات على دفعات قابلة للاستئناف + `restore.held`)؛ `backup-client.tsx` (5 حالات) و`restore-client.tsx` (6 حالات)؛ `AppNav` «نسخة محلية»/«استعادة»؛ `e2e/sys-backup.spec.ts` (تصدير حقيقي ثم استعادة بـ`setInputFiles`) |
| T1.38 SYS-07 (5) + SYS-08 (4) | `/sync/recovery?id=`، `/sync/epoch` | `RecoveryAuthentication` (رمز التجديد؛ الهوية من الجلسة لا الحمولة)؛ `sync/recovery.py`: `POST /api/sync/recovery/handover` (حجر `recovered` + `actor_user`)، `GET /api/sync/recovery` (المالك تفصيل / غيره عدد وقيمة)، `POST .../{device}/restore|freeze|wipe` (استرداد بخطّ PUSH منسوباً لمُنشئه؛ `FROZEN`/`WIPED` + إقرار مكتوب؛ هجرات core 0010 وsync 0008)، `POST /api/sync/reconcile` (موجودة/مفقودة بالهويات)؛ `device_wiped` في المصادقة | `pusher.ts` يهمل ردّ الجيل القديم (`isStaleEpoch`)؛ `lib/sync.ts`: `onHalt` (تسجيل تغيّر الجيل في `sync.epoch_change`، تسليم المعلّق عند السحب، محو محلي عند `device_wiped`)، `handOverToQuarantine`، `reconcileEpoch` (`adoptEpoch` → reconcile → رفع المفقود)؛ `recovery-client.tsx` (5 حالات) و`epoch-client.tsx` (4 حالات)؛ `e2e/sys-recovery.spec.ts` |
| T1.39 SYS-09 (5) + SYS-10 (6) | `/sync/update?updated=`، `/sync/import` | `GET /api/app/update` (الإصدار المتاح من إعدادات النشر)؛ `catalog/imports.py` + `DataImportBatch` (هجرة catalog 0005): `POST /api/imports/preview` (مطابقة مقترحة، بصمة الملف)، `.../{id}/decide` (المكرر بسعرين)، `.../{id}/apply` (دفعات قابلة للاستئناف؛ `stop_after` خلف الحارس)، `.../{id}/revert` (أصناف فقط، 24 ساعة)، `.../{id}/rejected.csv`؛ `wipe_scenario` يمحو دفعات الاستيراد والأطراف | `update-client.tsx` (ready/pending_sync/validation_error/success/server_error؛ `backup.last_exported_at` من SYS-05؛ `app.update`) و`import-client.tsx` (ready/loading/validation_error/partial/success/server_error؛ `SelectField` للمطابقة)؛ أسماء الملفات بلا mono؛ `e2e/sys-update-import.spec.ts` |
| T1.40 WEB-01 (4) | `/install` | — | `public/sw.js` (هيكل + أصول + خطوط؛ `/api/` و`/portal/` شبكة فقط؛ لا `skipWaiting` إلا برسالة الصفحة)؛ `lib/pwa.ts` (`setupPwa`، `registerServiceWorker` في الإنتاج أو `?sw=1`، `installEnv`، `promptInstall` بلا تكرار في الجلسة، `applyUpdate` يرفض مع معلّق)؛ `PwaSetup` في `providers.tsx`؛ `features/web/install-client.tsx` (ready/permission_denied/offline/success)؛ `AppNav` «التثبيت»؛ `e2e/web-install.spec.ts` (بيئات بوكيل المستخدم + عامل خدمة حقيقي في Chromium) |
| T1.41 WEB-02 (4) | `/notify` | `core.DeviceEndpoint` (0011 + RLS؛ `endpoint_sha256` فريد لكل مستأجر؛ اشتراك واحد فعّال لكل جهاز)؛ `core/push.py` (`NOTIFICATION_KINDS` مغلقة بعناوين ثابتة، `subscribe`/`unsubscribe`/`status_payload` بلا نقطة نهاية، `send` عبر `pywebpush` مع 404/410 → منتهٍ، `notify_device`)؛ `core/push_views.py` `GET /api/push/status`، `POST /api/push/subscribe|unsubscribe|test` (جلسة جهاز فقط)؛ VAPID من `STING_VAPID_PUBLIC_KEY/PRIVATE_KEY/SUBJECT` | `public/sw.js` (`push`/`notificationclick`/`pushsubscriptionchange` → `PUSH_EXPIRED`)؛ `lib/pwa.ts` (`pushSupported`/`pushPermission`/`requestPushPermission`/`subscribePush`/`unsubscribePush`/`currentPushSubscription` + `__stingPushFake` للاختبار)؛ `features/web/notify-client.tsx` (ready/permission_denied/expired/success؛ تجديد صامت عند إذن قائم؛ إشعار تجريبي فوراً بعد الاشتراك)؛ `AppNav` «الإشعارات»؛ `e2e/web-notify.spec.ts`؛ `core/tests/test_push.py` | 0005 §٤٤ |
| T1.42 WEB-03 (4) | `/print` | — (لا خادم؛ الطرازات بيانات خارجية `public/printers/models.json` G-10) | `lib/print/raster.ts` (تشكيل على لوحة → أحادي → `GS v 0`؛ صفحة تجربة: متصل/مختلط/أرقام/Code 39/كشف 12)؛ `lib/print/ble.ts` (Web Bluetooth BLE/GATT فقط، `getAvailability` قبل الزر R-09، كتابة بحزم، failed/unknown؛ `__stingBleFake`)؛ `lib/print/printers.ts` (سجل الطابعات في meta `print.printers`، `sendTestPage` نص/صورة، `printReceiptOnDevice` الناقل الفعلي للإيصال)؛ `features/web/print-client.tsx` (ready/validation_error/server_error/success + جدول الطابعات وبنود التحقق)؛ `receipt-client.tsx` يطبع عبر BLE إن ثبتت طابعة؛ `AppNav` «الطابعة»؛ `@types/web-bluetooth`؛ `e2e/web-print.spec.ts`؛ `docs/evidence/printers.md` (ACC-83 غير مُثبت) | 0005 §٤٥ |
| T1.43 البوابة (7 سيناريوهات آلية) | `apps/web/e2e-gate/*` + `playwright.gate.config.ts` (Django 8100 + Next 3100 بتمرير `/api/*`) | `core/scenario/seed.py` (عميل + افتتاحي سكر 10 + حدّ ائتمان؛ محو كامل)؛ `sync/bootstrap.py` (parties في النسخة)؛ `sales|parties|shifts/providers.py` (مؤشرات الرئيسية الثلاثة)؛ `sync/views.py` (drop_ack/freeze_reconciliation/network_cut فعلياً)؛ `sync/tests/test_push.py::TestFaults` | `next.config.ts` rewrites؛ `login-client`/`select-org` يحملان `next`؛ `shifts/open-client` رابط نقطة البيع عند وردية مفتوحة؛ `e2e-gate/fixtures.ts` (جهاز = سياق؛ تنقّل بالروابط؛ قطع الشبكة بإسقاط `/api/*`)؛ CI job `gate`؛ `docs/evidence/README.md` | 0005 §٤٦ |
| T2.1 ORG-01 (6) + ORG-02 (4) | `/org/users`، `/org/roles` | `core.RolePermission` (0012 + RLS)؛ `core/org.py` (المفاتيح العشرة وافتراضات الإطار، `ensure_matrix` كسولاً، `matrix_payload`/`save_matrix` بعمود مالك مقفل وعدّ المتأثرين، `users_payload` بحالات الدعوة الأربع، `invite`/`resend`/`revoke` بلا دعوة ثانية)؛ `core/org_views.py` `GET /api/org/users`، `POST /api/org/invitations`، `POST /api/org/invitations/{id}/resend|revoke`، `GET|PUT /api/org/roles`؛ `sales.caps_for` يقرأ سقف الخصم من المصفوفة | `features/org/users-client.tsx`، `roles-client.tsx`، `mono.tsx` (`MonoText`)، `org.css`؛ `AppNav` «المستخدمون»/«الأدوار»؛ `e2e/org-users-roles.spec.ts`؛ `core/tests/test_org.py` (3) | 0005 §٤٧ |
| T2.2 ORG-03 (4) + ORG-04 (5) | `/org/branches`، `/org/devices` | `core/org_branches.py` (ما يحمله الفرع: فواتير/ورديات/أصناف برصيد/ذمم؛ `code_locked` بعد أول فاتورة؛ إنشاء بالرمز؛ إقفال بشروط؛ الحذف `delete_unavailable`؛ صفوف الأجهزة بآخر ظهور والمعلّق المبلَّغ وحالة اتصال حسابية)؛ `GET|POST /api/org/branches`، `POST /api/org/branches/{id}/update|close|delete`، `GET /api/org/devices` | `features/org/branches-client.tsx`، `devices-client.tsx` (كاش `org.devices_cache` للحالتين offline/stale)، `ago.tsx`؛ `AppNav` «الفروع»/«الأجهزة»؛ `e2e/org-branches-devices.spec.ts`؛ `core/tests/test_org_branches.py` (2) | 0005 §٤٨ |
| T2.3 ORG-05 (5) | `/org/users/{id}/revoke` (من «سحب الوصول» في ORG-01) | `User.deactivated_*` (0013)؛ `core/org_revoke.py` (`preview` بالنطاقات والأجهزة المشتركة والمعلّق المبلَّغ والوردية المفتوحة كمانع؛ `disable_user` now/after_upload بلا مسّ جلسات الآخرين؛ `revoke_branch` للمدير في فرعه؛ `wipe_device` عبر SYS-07 بإقرار)؛ `GET|POST /api/org/users/{id}/revocation` | `features/org/revoke-client.tsx` (ready/validation_error/permission_denied/pending_sync/success)؛ `e2e/org-revoke.spec.ts`؛ **بوابة**: `e2e-gate/gate-org.spec.ts` ACC-63 على جهاز مشترك بتبديل المستخدم (`switchUser`)؛ `core/tests/test_org_revoke.py` (2) | 0005 §٤٩ |
| T2.4 ORG-06 (4) + ORG-08 (3) | `/org/subscription`، `/org/subscription/expiry` | `core.TenantSubscription` (0014 + RLS؛ تجريبية 30 يوماً كسولاً)؛ `core/subscription.py` (`PLANS` بقيم الإطار، `has_feature` المركزي مع تدرّج §١١.٢، `can_register_device`/`can_add_branch`، `entitlements_payload` بمبالغ للمالك فقط، `expiry_payload` بقائمتي G-08، `set_for_scenario`)؛ `GET /api/org/subscription`، `GET /api/org/subscription/expiry`؛ حدّ الأجهزة في التسجيل وORG-04، حدّ الفروع في ORG-03؛ `POST /api/scenario/subscription` | `features/org/subscription-client.tsx`، `expiry-client.tsx`؛ `AppNav` «الاشتراك»؛ `e2e/org-subscription.spec.ts`؛ `core/tests/test_subscription.py` (4) | 0005 §٥٠ |
| T2.5 ORG-07 (5) | `/org/subscription/renew` (من «تجديد الاشتراك» في ORG-06/08) | `core.SubscriptionProof` (0015 + RLS؛ رقم فريد، صورة base64 ≤ 2 MB)؛ `core/subscription.py` (`submit_proof` معلّق بلا تفعيل، `attach_image` المسار البديل، `review_proof` يمدّد 30 يوماً مرة واحدة أو يرفض بسبب، `due_payload`)؛ `GET|POST /api/org/subscription/proofs`، `POST …/proofs/{id}/image`، `POST /api/platform/tenants/{t}/subscription-proofs/{id}/review` (موظف المنصة) | `features/org/renew-client.tsx` (ready/validation_error/saving/success/server_error؛ الصورة تُحفظ في meta عند فشل الرفع)؛ `e2e/org-renew.spec.ts`؛ `core/tests/test_subscription_proof.py` (2) | 0005 §٥١ |
| T2.6 ORG-09 (4) + ORG-10 (4) | `/org/settings` («الإعدادات»)، `/org/audit` («سجل التدقيق») | `core.AuditEvent` + `TenantSettings.version/updated_by_name` (0016)، `core.OwnershipTransfer` + `User.former_owner_until` (0017)؛ `core/org_settings.py` (حفظ بالإصدار → `conflict`، `too_wide` بالقطع، طريقة النقد لا تُعطَّل، نقل الملكية بموانعه و24 ساعة)؛ `core/audit.py` (`record`/`query`/`export_csv`، نطاق all/branch/own)؛ خطّافات التدقيق في org/org_revoke/org_branches/subscription/shifts/parties/sync.review؛ `GET|PUT /api/org/settings`، `GET /api/org/audit[?export=csv]`، `GET|POST /api/org/ownership`، `POST …/ownership/{id}/{confirm|cancel}` | `features/org/settings-client.tsx` (ready/validation_error/success/conflict؛ معاينة `Receipt` فورية؛ لوحة نقل الملكية)، `audit-client.tsx` (loading/ready/empty/permission_denied)؛ `e2e/org-settings-audit.spec.ts`؛ `core/tests/test_org_settings_audit.py` (3) | 0005 §٥٢ |
| T2.7 REP-01 (6) + REP-02 (5) | `/reports` («تقرير المبيعات» — «التقارير» في تنقّل POS)، `/reports/receivables` («تقرير الذمم») | `sales/reports.py` (`sales_report` بالمدى والنطاق والوسيلة، يستبعد المعكوس، `completeness` من المعلّق المبلَّغ وآخر ظهور، `export_csv` بسطر الاكتمال)؛ `parties/reports.py` (`receivables_report`: رصيد/آخر سداد/أقدم حركة غير مغطّاة/المصدر، بلا أعمار G-15)؛ `core/report_views.py`: `GET /api/reports/sales[?range&start&end&branch_id&method&export=csv]`، `GET /api/reports/receivables[?side&export=csv]` (403 لغير المالك والمدير) | `features/reports/sales-report-client.tsx` (ready/loading/empty/stale/pending_sync/permission_denied؛ معلّق هذا الجهاز يُضاف موسوماً؛ كاش meta)، `receivables-report-client.tsx` (ready/loading/empty/stale/permission_denied)؛ `e2e/reports.spec.ts`؛ `core/tests/test_reports.py` (2 — يطابق HOME-01) | 0005 §٥٣ |
| T2.8 REP-03 (5) + REP-04 (5) | `/reports/stock` («تقرير المخزون»)، `/reports/cash` («تقرير الصندوق») | `inventory/reports.py` (`stock_report`: أول المدة/وارد/صادر/الرصيد بوحدة البيع من `StockMovement` للفرع، المستند المفسِّر، السالب بسببه، يطابق INV-01)؛ `shifts/reports.py` (`cash_report`: المغلقة وحدها، الفارق والصافي، المفتوحة المهجورة، `conflict` من الحجر)؛ **`sync.appliers.BusinessConflict`** + `apply_shift_closed` يحجر الإقفال الثاني `shift_closed_twice`؛ `GET /api/reports/stock|cash[?range&start&end&branch_id&export=csv]` | `features/reports/stock-report-client.tsx` (ready/loading/empty/stale/pending_sync؛ معلّق هذا الجهاز موسوم)، `cash-report-client.tsx` (ready/loading/empty/conflict/stale)؛ `e2e/reports-stock-cash.spec.ts`؛ `core/tests/test_reports_stock_cash.py` (2) | 0005 §٥٤ |
| T2.9 REP-05 (4) + REP-06 (5) | `/reports/margin` («الهامش»)، `/reports/export` («تصدير تقرير») | `sales/margin.py` (`margin_report`: phase_locked بلا سياسة، empty بأصناف بلا تكلفة، ready بالفرع والمجموعة بالنقاط الأساسية؛ `TenantSettings.inventory.cost_policy` عبر `PUT /api/org/settings`)؛ `core.ReportExport` (0018 + RLS) + `core/report_export.py` (معاينة الصفحة الأولى، توليد HTML/CSV، ملخص شهري للمبيعات، حدّ 12 شهراً بالبدائل، حصة شهرية، تنزيل بالرابط المخوَّل)؛ `GET /api/reports/margin`، `GET /api/reports/export/preview`، `GET|POST /api/reports/exports`، `POST …/exports/limit-request`، `GET …/exports/{token}` | `features/reports/margin-report-client.tsx` (phase_locked/empty/ready/permission_denied)، `export-client.tsx` (ready/loading/validation_error/success/server_error؛ `DocPreview` بـiframe `srcDoc`)؛ `e2e/reports-margin-export.spec.ts`؛ `core/tests/test_reports_margin_export.py` (2) | 0005 §٥٥ |
| T2.10 NOT-01 (5) + NOT-02 (4) | `/notify/inbox` («الوارد»)، `/notify/inbox/{id}` (رابط عميق)، `/notify/preferences` («تفضيلات التنبيه») | `core.Notification` + `core.NotificationPreference` (0019 + RLS)؛ `core/notifications.py` (`refresh` يشتقّ التشغيلي/الحسابي/التسويقي بمفتاح إزالة تكرار ويوسم ما زال سببه، `emit`، `inbox` بالترتيب التشغيلي أولاً، `open_link` بصلاحية الرابط وإعادة فحص التخويل، `mark_read` لكل مستخدم، `prefs_payload`/`save_prefs` بالقناة بلا وجهة والعدّ)؛ `subscription.review_proof` يبثّ إشعاراً حسابياً؛ `GET /api/notifications[?category]`، `POST …/{id}/read|open`، `GET|PUT /api/notifications/preferences` | `features/notify/inbox-client.tsx` (ready/loading/empty/expired/permission_denied؛ `TimeList` بالفئات)، `preferences-client.tsx` (ready/validation_error/offline/success؛ meta `not.prefs_pending`)؛ `e2e/notify-inbox-prefs.spec.ts`؛ `core/tests/test_notifications.py` (2) | 0005 §٥٦ |
| T2.11 NOT-03 (4) + NOT-04 (4) | `/notify/campaigns` («الحملات»)، `/notify/campaigns/new`، `/notify/campaigns/{id}` | `core.Campaign` (0020 + RLS)، `Party.marketing_consent_at/opt_out_at` (parties 0007)؛ `core/campaigns.py` (`audience` يُحصى خادمياً بالشرائح الأربع وإزالة التكرار ومن أوقف التسويق ويرفض جمهور مستأجر آخر بلا تسريب، `parts_for` 160/153، `quota` من الباقة، `blockers` الخمسة، `preview`، `save` مسودة، `list_payload` بالرقم الصادق)؛ `GET|POST /api/campaigns`، `POST /api/campaigns/preview`، `GET|PUT /api/campaigns/{id}` | `features/notify/campaigns-client.tsx` (ready/loading/empty/permission_denied؛ نتائج بأربع درجات وأسباب الفشل)، `campaign-new-client.tsx` (ready/empty/validation_error/permission_denied؛ اسم المحل يُدرج تلقائياً)؛ `e2e/notify-campaigns.spec.ts`؛ `core/tests/test_campaigns.py` (1) | 0005 §٥٧ |
| T2.12 NOT-05 (5) + NOT-06 (5) | `/notify/campaigns/{id}/approve` (NOT-05)، `/notify/campaigns/{id}` (NOT-06) | `core.CampaignMessage` (0021 + RLS، صفّ لكل طرف مرة واحدة)؛ `core/campaigns.py`: `verify_for_send` (إعادة التحقق قبل كل محاولة)، `approve` (جدولة تُسجَّل ولا تُرسل / إرسال الآن)، `run_due` عامل كسول عند الجلب (المتأخر > 30 دقيقة يُسأل)، `dispatch` (الطابور فقط — إعادة التشغيل لا تكرّر؛ من أوقف التسويق يُحترم)، محاكاة المزوّد بمفاتيح `sms_provider_silent/sms_temp_reject/sms_worker_cut`، `results_of` بالدرجات الأربع بلا «قُرئت»، `cancel` (ما أُرسل لا يُستردّ)، `retry_temporary` (تخصم مرة أخرى)؛ `POST /api/campaigns/{id}/verify|approve|cancel|retry`؛ `POST /api/scenario/marketing` | `features/notify/campaign-approve-client.tsx` (ready/validation_error/saving/expired/success)، `campaign-results-client.tsx` (ready/empty/partial/server_error/success)؛ `e2e/notify-campaign-flow.spec.ts`؛ `e2e-gate/gate-campaigns.spec.ts` (ACC-105/109/103)؛ `core/tests/test_campaign_dispatch.py` (2) | 0005 §٥٨ |
| T2.13 PUR-01 (4) + PUR-02 (4) | `/purchasing/orders` («أوامر الشراء»)، `/purchasing/orders/new`، `/purchasing/orders/{id}` | `inventory.PurchaseOrder/PurchaseOrderLine` (inventory 0009 + RLS؛ أونلاين، لا حركة مخزون ولا ذمة)؛ `inventory/purchasing.py` (`preview` بالمكافئ بوحدة البيع والرصيد و«يكفي N أيام» والسعر التقديري من آخر شراء، أسئلة السطر `unit_required/supplier_mismatch/qty_required`، `create`، `send` بفعل منفصل ومفتاح العطل `po_send_fail`، `cancel` بسبب، `list_payload` بحجب عمود القيمة عن أمين المخزن)؛ `GET|POST /api/inventory/purchasing/orders`، `POST …/orders/preview`، `GET …/orders/{id}`، `POST …/orders/{id}/send|cancel` | `features/purchasing/orders-client.tsx` (ready/loading/empty/permission_denied)، `order-new-client.tsx` (ready/validation_error/saving/success؛ معاينة طازجة قبل الحفظ)؛ `e2e/purchasing-orders.spec.ts`؛ `inventory/tests/test_purchasing.py` (1) | 0005 §٥٩ |
| T2.14 PUR-03 (4) + PUR-04 (4) | `/purchasing/orders/{id}/document` (يبدأ المستند من الأمر)، `/purchasing/documents/{id}`، `/purchasing/documents/{id}/return`، `/purchasing/returns/{id}` | `inventory.PurchaseDocument/PurchaseDocumentLine/PurchaseReturn/PurchaseReturnLine` (inventory 0010 + RLS)؛ `inventory/purchase_docs.py` (`draft_from_order`، `save_draft` بلا موانع، `blockers` = رقم الفاتورة/سبب الزيادة، `approve` مرة واحدة بالهوية → `GoodsReceipt PD-n` + `StockMovement receive` + تحديث الأمر partial/received + `effects` مخزون/تكلفة مرجّحة/ذمّة تستحق بعد 30 يوماً، `refer` → `referred` + إشعار للمالك، `record_return` بحدّ المستلَم−المردود وسعر المستند وسبب لكل سطر → `StockMovement purchase_return` + إشعار دائن، `respond` بالمقبول لكل سطر → partial/accepted/rejected)؛ مفتاح ORG-02 جديد `purchase_approve` (المدير حتى 3,000)؛ مزوّد ذمّة المورد = مستندات معتمدة − مرتجعات مقبولة؛ `POST /api/inventory/purchasing/orders/{id}/document`، `GET|PATCH …/documents/{id}`، `POST …/documents/{id}/approve|refer|return`، `GET|POST …/returns/{id}` | `features/purchasing/document-client.tsx` (ready/validation_error/permission_denied/success)، `return-client.tsx` (ready/validation_error/partial/success)؛ `e2e/purchasing-documents.spec.ts`؛ `inventory/tests/test_purchase_docs.py` (1) | 0005 §٦٠ |
| T2.15 PUR-05 (4) | `/purchasing/cost-margin` («التكلفة والهامش») | `inventory/cost_margin.py` (`report`: التكلفة للوحدة الأساسية من سطور الاستلام ذات التكلفة بسياسة G-03 أو المتوسط المرجّح افتراضاً، «مستند واحد/آخر سعر شراء/متوسط مرجّح · N»، لا هامش من وحدتين، ملاحظات `unit_mismatch/single_doc/negative/cost_rose/stable/manual`؛ `enable` مفتاح `inventory.purchasing_enabled` للمالك؛ `set_manual_cost` في `inventory.manual_costs`)؛ مفتاح ORG-02 `cost_margin_view` (المالك والمدير)؛ `GET|POST /api/inventory/purchasing/cost-margin` (`enable`/`manual_cost`) | `features/purchasing/cost-margin-client.tsx` (ready/empty/permission_denied/phase_locked)؛ `ui-web.PhaseLocked` بـ`onActivate`/`activateLabel`؛ `e2e/purchasing-cost-margin.spec.ts` (9/9)؛ `inventory/tests/test_cost_margin.py` (1) | 0005 §٦١ |
| T2.16 PUB-01 (2) + PUB-02 (3) + PUB-03 (3) + PUB-04 (3) | `/` لغير المسجَّل (PUB-01)، `/legal` (+`?section=market`)، `/status`، `/link-expired`، `/forbidden`، `not-found.tsx` | `core/public_views.py`: `GET /api/public/plans` (من `PLANS` + ما يُحجب عند الانتهاء)، `GET /api/public/legal` (البنود بحالتها — G-11)، `GET /api/public/status` (المكوّنات ووقت الفحص وإعلان الصيانة `STING_MAINTENANCE_*` وسجلّ `STING_STATUS_EVENTS`؛ مفتاح عطل `maintenance`)؛ `parties/views.py::StatementDocumentView` يحوّل المتصفح إلى `/link-expired` (302) لأي رابط غير صالح | `features/public/{about,legal,status}-client.tsx` + `pub-404.tsx` + `public-nav.tsx` + `public.css`؛ `root-client.tsx` → PUB-01 لغير المسجَّل؛ `e2e/public-pages.spec.ts` (21/21)؛ `core/tests/test_public.py` (1) | 0005 §٦٢ |
| T3.1 CUS-01 (4) + CUS-02 (4) | `/portal/{slug}` (+`?c=` رابط حملة مؤقت)، `/portal/{slug}/subscribe` — عامتان بلا جلسة | `core.PortalChannel`/`PortalSubscriber` (core 0022 + RLS)؛ `Campaign.valid_until`؛ `CampaignMessage.party_id` اختياري + `subscriber_id` + `read_at` والقيد بالرقم؛ `core/portal.py` (`ensure_channel` slug عشوائي، `channel_payload` رابط + QR SVG بـ`segno`، `page_payload` الإعلانات الثلاث والرابط المؤقت المنتهي، `subscribe` بتطبيع متساهل ورفض الناقص، `subscriber_payload`)؛ `campaigns.audience/_fill_outbox/dispatch` تضمّ المشتركين وتحترم إلغاءهم؛ `GET /api/portal/{slug}`، `POST …/subscribe`، `GET|PUT /api/org/portal` | `features/portal/shop-client.tsx` (loading/ready/empty/expired)، `subscribe-client.tsx` (ready/validation_error/permission_denied/success؛ الإذن بعد النقر و`__stingPushFake` في الاختبار)، `portal-store.ts`؛ `e2e/portal-shop-subscribe.spec.ts` (15/15)؛ `core/tests/test_portal.py` (1) | 0005 §٦٤ |
| T3.2 CUS-03 (5) + CUS-04 (3) + CUS-05 (3) | `/portal/{slug}/inbox` (+`?m=`، `?t=`)، `/portal/prefs`، `/portal/{slug}/status` — عامة بالرمز | `PortalSubscriber.channel_push/sms/inbox` (core 0023)؛ `core/portal.py` (`messages_payload` بحالة المزوّد والانتهاء، `mark_read`، `update_prefs` → `all_channels_off`، `unsubscribe`، `resubscribe` 7 أيام → `undo_expired`، `me_payload`)؛ `GET|PUT|DELETE /api/portal/{slug}/me?t=`، `POST …/resubscribe`، `GET …/messages`، `POST …/messages/{id}/read` (403 لمشترك آخر) | `features/portal/inbox-client.tsx` (loading/ready/empty/expired/permission_denied)، `prefs-client.tsx` (ready/validation_error/success؛ «إلغاء الكل» بحوار)، `status-client.tsx` (ready/permission_denied/expired)؛ `portal-store.ts` (`portal.shops`)؛ `e2e/portal-inbox-prefs.spec.ts` (12/12)؛ `core/tests/test_portal.py` (2) | 0005 §٦٥ |
| T3.3 MP-08 (5) + MP-09 (5) | `/market/seller` («دور البائع»)، `/market/profile` («صفحة المنشأة») | تطبيق `market` (0001 + RLS): `MarketAccount` (الدور، حالة التحقق، بنود القائمة، مستند السجل، أسباب النقص) و`MarketProfile` (مسوّدة + منشور)؛ `market/services.py` (`checklist`، `save_verification`، `submit_verification` → `checklist_incomplete`، `review_verification` verified/needs_more/rejected، `is_verified_seller`، `public_profile` من المنشور بالحقول المصرّح بها، `save_profile` → `service_areas_required`)؛ `GET|PUT /api/market/account`، `POST …/verification/submit`، `GET|PUT /api/market/profile`، `POST /api/platform/tenants/{t}/market-verification/review` | `features/market/seller-client.tsx` (ready/loading/validation_error/permission_denied/success)، `profile-client.tsx` (ready/validation_error/saving/permission_denied/success)، `market.css`؛ AppNav «دور البائع»/«صفحة المنشأة»؛ `e2e/market-seller-profile.spec.ts` (12/12)؛ `market/tests/test_seller.py` (1) | 0005 §٦٦ |
| T3.4 MP-10 (4) + MP-11 (5) | `/market/offers` («عروضي»)، `/market/offers/new`، `/market/offers/{id}` | `market.MarketOffer` (0002 + RLS)؛ مفتاح ORG-02 `market_publish` (المالك)؛ `market/offers.py` (`list_payload` بالعدّادات وما ينتهي هذا الأسبوع و`refresh_expiry`، `preview` ما يُنشر/ناقص/محجوب والبطاقة العامة، `save` مسودة، `publish` → `seller_not_verified`/`plan_feature_required`/`fields_missing`/`publish_permission_required`، `hide`)؛ `GET|POST /api/market/offers`، `POST …/preview`، `GET|PUT …/{id}`، `POST …/{id}/publish|hide` | `features/market/offers-client.tsx` (loading/ready/empty/expired)، `offer-edit-client.tsx` (ready/validation_error/saving/permission_denied/success؛ معاينة حيّة)؛ AppNav «عروضي»؛ `e2e/market-offers.spec.ts` (9/9)؛ `market/tests/test_offers.py` (1) | 0005 §٦٧ |
| T3.5 MP-12 (4) + MP-13 (5) | `/market/lists` («القوائم الخاصة»)، `/market/lists/{id}`، `/market/lists/new?offer=`، `/market/renewals` («تجديد التأكيد»، `?o=`) | market 0003 + RLS: `MarketPriceList` (شرائح)، `MarketPriceListMember` (دعوة/نشطة/موقوفة)، `MarketOffer.number/confirmed_at`؛ `market/price_lists.py` (`check_gaps` → `tier_gap`، سعر الوحدة بالمعامل، `invite_member` بمعرّف المنشأة، `accept_invite` بحساب المشتري، `visible_list_for` للنشط وحده وإلا 404)؛ `market/offers.py` (`save` يلغي التأكيد عند تغيير السعر/الوحدة/الحدّ، `renew` 48 ساعة بصلاحية `market_publish`، `renewals_payload` + `RENEWAL_RULES`)؛ `GET|POST /api/market/lists`، `GET|PUT …/{id}`، `POST …/{id}/members`، `POST …/members/{m}/suspend|resume`، `POST …/{id}/accept`، `GET /api/market/renewals`، `POST /api/market/offers/{id}/renew` | `features/market/lists-client.tsx` (ready/empty/validation_error/permission_denied)، `renewals-client.tsx` (ready/saving/expired/permission_denied/success)؛ AppNav؛ `e2e/market-lists-renewals.spec.ts` (12/12)؛ `market/tests/test_price_lists.py` (1) | 0005 §٦٨ |
| T3.6 MP-01 (5) + MP-02 (4) | `/market` («السوق»)، `/market/directory` («الدليل») — عامتان بلا حساب | `market/public.py` (`home` بالمنطقة والبحث: الموردون الناشرون في المنطقة بشاراتهم وعروضهم العامة السارية مجمّعة بالوحدة؛ `directory` بالمنطقة والفئة مع `alternatives` لأزرار المخرج؛ لا حقل غير منشور)؛ `GET /api/public/market?area=&q=`، `GET /api/public/market/directory?area=&category=` | `features/market/home-client.tsx` (loading/ready/empty/offline/stale؛ لقطة محلية بوقتها)، `directory-client.tsx` (loading/ready/empty/stale)، `market-store.ts`؛ القائمة العامة والتطبيق؛ `e2e/market-home-directory.spec.ts` (9/9)؛ `market/tests/test_public.py` (1) | 0005 §٦٩ |
| T3.7 MP-03 (4) + MP-04 (5) | `/market/suppliers/{tenant_id}` (رابط عام)، `/market/search?q=` | market 0004: `MarketAccount.verified_until/publish_suspended_at`، `MarketOffer.pickup_only/delivery_fee_minor/delivery_free_over_minor`؛ `market/services.badge_of` (verified/expired/suspended/none)؛ `market/public.py` (`supplier_profile` بالشارة وحدودها والحقائق صفراً والعروض العامة؛ `search` مجموعات بالعبوة، ترتيب بسعر الوحدة للمحسوم رسومه، غير المحسوم «قبل الرسوم»، المنتهي للسياق أسبوعاً، `multi_unit`)؛ `GET /api/public/market/suppliers/{id}`، `GET /api/public/market/search?q=&area=` | `features/market/supplier-client.tsx` (loading/ready/permission_denied/expired)، `search-client.tsx` (loading/ready/empty/stale/partial)؛ `e2e/market-supplier-search.spec.ts` (12/12)؛ `market/tests/test_public.py` (2) | 0005 §٧٠ |
| T3.8 MP-05 (5) + MP-06 (4) | `/market/offers/public/{id}` (رابط عام؛ الخاص بحساب مخوَّل)، `/market/following` («متابعاتي»؛ `?follow=` من MP-03) | market 0005: `MarketFollow` (منشأة متابِعة → مورد؛ نشطة/أُلغيت بوقتها)؛ `market/follows.py` (`offer_detail`: عام لأي زائر، خاص لعضو قائمة نشط بشرائحه، للمتابعين للمتابِع النشط، وإلا 404 نفسه؛ المخفي `withdrawn` مع عروض الناشر القائمة؛ `currency_mismatch`؛ `days_since_confirmed`؛ `follow`/`unfollow` للمالك وحده؛ `followers_count` في `profile_payload`)؛ `GET /api/market/offers/public/{id}`، `GET/POST /api/market/following`، `POST /api/market/following/{id}/unfollow` | `features/market/offer-detail-client.tsx` (ready/empty/expired/permission_denied/stale؛ الإضافة إلى السلة بتأكيد خادمي → `market.cart`)، `following-client.tsx` (ready/empty/permission_denied/success)؛ `e2e/market-offer-follow.spec.ts` (18/18)؛ `market/tests/test_follows.py` (1) | 0005 §٧١ |
| T3.9 MP-07 (4) + MP-14 (3) + MP-15 (4) | `/market/share?offer=|supplier=` («الروابط والدعوات»)، `/market/i/{token}` (عام)، `/market/unavailable?supplier=&offer=`، `/market/report?offer=|supplier=`، `/market/reports[/{id}]` («بلاغاتي») | market 0006: `MarketInvite` (share/invite/authorization؛ رمز هاشاً بعمر 7 أيام؛ عدّادات زيارة/تسجيل/نشر/أول طلب)، `MarketReport` (RP-n؛ سبب/دليل/حالة)؛ `market/links.py` (`share_preview` عامة بلا سعر خاص، `share`/`invite`/`revoke`، `request_authorization`/`incoming_authorizations`/`decide_authorization`، `resolve`/`accept`/`attribute`، `supplier_status`، `report`/`decide_report`)؛ مسارات `market/share/preview`، `market/invites[/{id}/revoke]`، `market/authorizations/incoming`، `public/market/invite/{token}`، `market/invite/{token}/accept`، `public/market/suppliers/{id}/status`، `market/reports[/{id}]` | `features/market/share-client.tsx` (ShareClient ready/permission_denied/success + InviteOpenClient ready/expired)، `unavailable-client.tsx` (ready/expired/permission_denied)، `report-client.tsx` (ready/validation_error/permission_denied/success)؛ روابط في MP-03/MP-05؛ `e2e/market-share-report.spec.ts` (21/21)؛ `market/tests/test_links.py` (1) | 0005 §٧٢ |
| T3.10 ORD-01 (5 مرسومة) + ORD-02 (6) | `/market/cart` («السلة»)، `/market/checkout?supplier=` | market 0007: `MarketOrder` (طلب/طلب سعر لمورد واحد؛ `op_id` متكرّر الأثر؛ `PO-n`؛ سطور نسخة بسعرها المؤكَّد؛ حالات ORD اللاحقة معرَّفة)؛ `market/orders.py` (`verify_lines` خادمياً: السعر الحالي/الوحدة/الحدّ الأدنى/الصلاحية/الرسوم/العملة/التعليق؛ `submit`: 409 `line_expired`/`price_changed`، 400 `currency_mismatch`/`min_order_not_met`/`one_supplier_per_order`، 403 بسقف `purchase_approve`؛ `RESPONSIBILITIES`، 72 ساعة)؛ `POST /api/market/orders/verify`، `GET/POST /api/market/orders` (`?op_id=`) | `features/market/cart-store.ts` (`market.cart`، `opIdFor`)، `cart-client.tsx` (ready/empty/offline/validation_error/stale)، `checkout-client.tsx` (ready/validation_error/saving/expired/success/server_error)؛ `playwright.config.ts` يقبل `PLAYWRIGHT_PORT`؛ `e2e/market-cart-checkout.spec.ts` (21/21)؛ `market/tests/test_orders.py` (1) | 0005 §٧٣ |
| T3.11 ORD-03 (5) + ORD-04 (4) | `/market/orders` («طلباتي»)، `/market/orders/incoming` («طلبات العملاء») | `market/orders.py`: `content_line`/`buyer_step`/`supplier_step`، `no_reply` محسوب (المهلة انقضت والحالة `sent` — لا «مرفوض»)، `near_deadline` (<6 س)، `orders_payload` (+`awaiting_count`/`hidden_by_filter`)، `incoming_payload` (طرف المورد بـ`platform_context`؛ بالمهلة لا بالتاريخ؛ العدّادات)، `resend` (المهلة من جديد، الإصدار +1)؛ `GET /api/market/orders/incoming`، `POST /api/market/orders/{id}/resend` | `features/market/orders-client.tsx` (loading/ready/empty/stale/pending_sync؛ الفلتر يُعلن ما يُخفي؛ المسودة المحلية صفاً)، `incoming-client.tsx` (loading/ready/empty/expired؛ رقائق بالمهلة)؛ `e2e/market-orders-lists.spec.ts` (15/15)؛ `market/tests/test_orders.py` (2) | 0005 §٧٤ |
| T3.12 ORD-05 (5) + ORD-06 (6) | `/market/orders/{id}` (لطرفيه)، `/market/orders/{id}/quote` (المورد) | market 0008: `MarketOrderVersion` (request/quote/revision؛ مسودة بلا `sent_at`؛ `accepted_at`)، `MarketOrderEvent` (الخط الزمني)، `MarketOrder.agreed_version`؛ `market/order_flow.py` (`load_order` كلٌّ طرفه، `open_request_version` من ORD-02، `detail_payload` بسلّم الكميات وقيمتَي المستلم/الفارق و`conflict`/`partial`، `quote_payload` بـ`catalog_expired`، `save_quote` مسودة/إرسال بتحقق الصلاحية والزيادة، `decline` بسبب، `accept_version`)؛ `GET /api/market/orders/{id}`، `GET/POST …/quote`، `POST …/decline`، `POST …/accept` | `features/market/order-detail-client.tsx` (loading/ready/partial/conflict/permission_denied؛ `OrderTimeline`)، `quote-client.tsx` (ready/validation_error/saving/expired/success/partial)؛ `e2e/market-order-detail-quote.spec.ts` (18/18)؛ `market/tests/test_order_flow.py` (1) | 0005 §٧٥ |
| T3.13 ORD-07 (5) + ORD-08 (4) | `/market/orders/{id}/compare?version=` (المشتري)، `/market/orders/{id}/ship` (المورد) | market 0009: `MarketShipment` (SH-NN؛ سطور؛ ناقل/وصول اختياريان؛ `received_at`)، `MarketOrderVersion.delivery_days/rejected_at`؛ `order_flow.py`: `compare_payload` (طلبك/المعروضة/الأحدث بفرق مسمّى ومجاميع وثواني الصلاحية)، `accept_version` يرفض الأقدم `version_superseded`، `reject_version` بسبب، `requote`، `shipments_payload`، `ship` (الحدّ الصلب `exceeds_confirmed`؛ يكتب `qty_shipped`؛ «مشحون جزئياً — N%» ثم «سُلِّم»)؛ `GET …/compare`، `POST …/reject`، `POST …/requote`، `GET/POST …/shipments` | `features/market/compare-client.tsx` (ready/validation_error/expired/conflict/success؛ عدّاد الصلاحية)، `ship-client.tsx` (ready/validation_error/partial/success)؛ روابط في ORD-05؛ `e2e/market-compare-ship.spec.ts` (15/15)؛ `market/tests/test_order_flow.py` (2) | 0005 §٧٦ |
| T3.14 ORD-09 (5) + ORD-10 (5) | `/market/orders/{id}/receive?shipment=`، `/market/orders/{id}/cancel-remaining` | market 0010: `MarketOrder.remaining_cancelled_at/cancel_reason`، `MarketShipment.received_lines/received_by_name/dispute_opened`؛ `order_flow.py`: `receive_payload`/`receive` (العدّ ≤ المشحون `exceeds_shipped`؛ الزيادة بسبب تُقصّ وتُسجَّل؛ الرفض بسبب ومصير؛ `open_dispute` → «خلاف مفتوح»؛ `already_received`؛ يكتب `qty_received/qty_rejected`)، `cancel_breakdown`/`cancel_remaining` (المؤكَّد غير المشحون فقط؛ سبب؛ صلاحية `purchase_approve`؛ «مكتمل جزئياً — أُلغي المتبقّي»)، `request_cancel`؛ `GET/POST …/receive`، `GET/POST …/cancel-remaining` | `features/market/receive-client.tsx` (ready/validation_error/partial/conflict/success)، `cancel-remaining-client.tsx` (ready/validation_error/partial/permission_denied/success)؛ روابط في ORD-05؛ `e2e/market-receive-cancel.spec.ts` (9/9)؛ `market/tests/test_order_flow.py` (3) | 0005 §٧٧ |
| T3.15 ORD-11 (4) + ORD-12 (5) | `/market/orders/{id}/return`، `/market/orders/{id}/disputes?d=` | market 0011: `MarketReturn` (RT-NN؛ سطور بكمية موافَق عليها؛ requested/approved/partial/rejected/executed)، `MarketDispute` (DSP-n؛ الدور ومهلته؛ أدلة الطرفين؛ نتيجة ومرجع؛ وسيط)؛ `market/disputes.py` (`returns_payload`/`request_return` بـ`exceeds_returnable`، `decide_return`، `open_dispute` من ORD-09، `disputes_payload`/`add_evidence` يقلب الدور، `accept_supplier_figure` بحدّ ORG-02، `close_dispute` لا يحرّك دفتراً، `request_mediator`)؛ `GET/POST …/returns`، `POST …/returns/{id}/decide`، `GET …/disputes`، `POST …/disputes/{id}/{evidence|accept-supplier|close|mediator}`؛ `open_disputes` في ORD-05 | `features/market/return-client.tsx` (ready/validation_error/partial/success؛ المورد يقرّر من الصفحة نفسها)، `disputes-client.tsx` (ready/empty/partial/permission_denied/success)؛ روابط في ORD-05؛ `e2e/market-return-dispute.spec.ts` (12/12)؛ `market/tests/test_order_flow.py` (4) | 0005 §٧٨ |
| T3.16 ORD-13 (5) + ORD-14 (4) + ORD-15 (4) | `/market/orders/{id}/payment`، `/market/orders/recover?op=`، `/market/orders/{id}/restore` | market 0012: `MarketPayment` (PAY-n؛ مرجع فريد لكل مشترٍ/مورد؛ توزيع؛ recorded/matched/rejected)، `MarketOrder.paid_minor/restore_point/reconciling`، `MarketOrderEvent.needs_decision/decision/decision_reason`؛ `market/settlement.py` (`due_minor` من المستلَم − المطابَق، `record_payment` بـ`reference_used`/`amount_mismatch`/`allocation_mismatch`، `match_payment` بتاريخين، `remind_payment`، `probe` بلا إنشاء وبفرق الحمولة، `restore`/`decide_event` للمالك وتوقف الشحن/الاستلام `reconciling`)؛ `GET/POST …/payments`، `POST …/payments/{id}/{match|reject|remind}`، `POST market/orders/probe`، `GET/POST …/restore`، `POST …/restore/events/{id}`؛ ORD-02 تحفظ `market.checkout.attempt.{op}` | `features/market/payment-client.tsx` (ready/validation_error/partial/stale/success)، `recover-client.tsx` (ready/conflict/server_error/success)، `restore-client.tsx` (conflict/stale/permission_denied/success)؛ روابط في ORD-02/ORD-05؛ `e2e/market-payment-recover-restore.spec.ts` (21/21)؛ `market/tests/test_order_flow.py` (5) | 0005 §٧٩ |
| T3.17 LINK-01…05 + GROW-01…03 `phase_locked` | `/market/link/{parties,items,receipts,documents,returns}`، `/grow/{replenish,suppliers,promote}` | — (لا منطق ولا API) | `features/phase/phase-screens.ts` (جدول النصوص الحرفية: العنوان/التلميح/صيغة القفل/البديل الحاضر/لقطة «مثال»)، `phase-screen-client.tsx` (`PhaseLocked` بمحتوى `inert` وزرّ معطَّل بتفسير)؛ روابط من ORD-01/ORD-09/ORD-15؛ `e2e/phase-link-grow.spec.ts` (24/24) | 0005 §٨٠ |
| T3.18 PLT-01 (3) + PLT-02 (4) | `/platform/login`، `/platform/tenants`، `/platform/tenants/{id}` | تطبيق `stingops` (0001): `OperatorProfile` (مستخدم منصة + TOTP)، `OperatorAccessLog`، `SupportGrant`؛ `core/auth/totp.py`؛ `stingops/services.py` (`login`: invalid_credentials/otp_required/otp_invalid/tenant_account، `tenants_payload` بمرشّحات بلا دفاتر، `tenant_detail` مدقَّق، `grant_support`)؛ `POST /api/platform/login`، `GET /api/platform/tenants`، `GET /api/platform/tenants/{id}`، `POST /api/org/support-access` | `features/platform/{operator-session.ts, platform-nav.tsx, operator-login-client.tsx (ready/validation_error/permission_denied), tenants-client.tsx (loading/ready/empty/permission_denied), tenant-detail-client.tsx}`؛ `e2e/platform-login-tenants.spec.ts` (9/9)؛ `stingops/tests/test_operator.py` (1) | 0005 §٨١ |
| T3.19 PLT-03 (4) + PLT-04 (4) | `/platform/proofs`، `/platform/announcements` | `stingops` (0002): `ProofClaim` (حجز 15 دقيقة + طلب تسليم)، `PlatformAnnouncement`؛ `stingops/review.py` (`proofs_payload`، `open_proof`، `proof_image` مسجَّلة، `request_handover`، `review` بفحص `reference_used` قبل الاعتماد و`claimed_by_other`/`already_reviewed`/`reason_required`؛ `audience_preview` بشرائح حيّة و`promise_outside_plan`، `save/schedule/cancel_announcement`، `public_maintenance_notice` → PUB-03)؛ `GET /api/platform/proofs?status=`، `POST …/proofs/{tenant}/{proof}/{open|image|handover|approve|reject}`، `GET/POST /api/platform/announcements`، `POST …/announcements/preview`، `POST …/announcements/{id}/{schedule|cancel}` | `features/platform/{proofs-client.tsx (ready/validation_error/conflict/success), announcements-client.tsx (ready/validation_error/saving/success), platform-nav.tsx}`؛ `e2e/platform-proofs-announcements.spec.ts` (15/15)؛ `stingops/tests/test_operator.py` (2) | 0005 §٨٢ |
| T3.20 PLT-05 (4) + PLT-06 (5) | `/platform/outbound`، `/platform/verifications` | `stingops` (0003): `ChannelState` (تعذّر/عودة معلَنة باسم المشغّل)؛ `stingops/ops.py` (`outbound_payload` من `CampaignMessage`/`DeviceEndpoint` عبر المستأجرين + حصة `STING_CAMPAIGN_DAILY_QUOTA`؛ `set_channel_state`؛ `verifications_payload` بالأقدم أولاً ومتوسط المراجعة؛ `verification_document` مسجَّل؛ `decide_verification` يلفّ `market.services.review_verification`)؛ `GET /api/platform/outbound`، `POST …/outbound/channels/{key}`، `GET /api/platform/verifications`، `POST …/verifications/{tenant}/{document|decide}` | `features/platform/{outbound-client.tsx (ready/empty/partial/server_error), verifications-client.tsx (loading/ready/empty/validation_error/success), platform-nav.tsx}`؛ `e2e/platform-outbound-verifications.spec.ts` (12/12)؛ `stingops/tests/test_operator.py` (3) | 0005 §٨٣ |
| T3.21 PLT-07 (4) + PLT-08 (4) | `/platform/reports`، `/platform/disputes` | `market` 0013 (`MarketOffer.suspended_*` + الاعتراض، `MarketDispute.mediator_note/referred_external_at`)؛ `stingops/moderation.py` (`reports_payload`، `decide_report` suspend/close/ledger→403، `decide_appeal` بغير من علّق، `disputes_payload` بحدّ تدخّل 14 يوماً، `suggest_path`، `refer_external`)؛ `offers.appeal_suspension` + استبعاد المعلَّق من `public.search`/التفصيل العام ومنع `publish`؛ `GET /api/platform/reports?status=`، `POST …/reports/{tenant}/{id}/decide`، `POST …/appeals/{tenant}/{offer}/decide`، `GET /api/platform/disputes`، `POST …/disputes/{tenant}/{id}/{suggest|refer}`، `POST /api/market/offers/{id}/appeal` | `features/platform/{reports-client.tsx (ready/validation_error/permission_denied/success), disputes-ops-client.tsx (loading/ready/empty/partial), platform-nav.tsx}`؛ `e2e/platform-reports-disputes.spec.ts` (9/9)؛ `stingops/tests/test_operator.py` (4) | 0005 §٨٤ |
| T3.22 PLT-09 (4) + PLT-10 (4) | `/platform/health`، `/platform/backups` | `stingops` 0004 (`ServerBackup`، `RestoreDrill`)؛ `stingops/metrics.py` (`LatencyMiddleware` p95 آخر 5 دقائق — في `MIDDLEWARE`)؛ `stingops/health.py` (`health_payload` من الجلسات/العمليات/الحجر بلا محتوى، `sync_component_state` → PUB-03، `backups_payload`، `record_backup`، `run_isolated_drill`، `request_live_restore` بالشروط الأربعة ولا تنفيذ)؛ `GET /api/platform/health`، `GET /api/platform/backups`، `POST …/backups/{id}/{drill|live}` | `features/platform/{health-client.tsx (loading/ready/stale/server_error — قياس كل 30 ثانية), backups-client.tsx (loading/ready/server_error/success), platform-nav.tsx}`؛ `e2e/platform-health-backups.spec.ts` (12/12)؛ `stingops/tests/test_operator.py` (5) | 0005 §٨٥ |
| T3.23 PLT-11 (4) + PLT-12 (4) | `/platform/m0`، `/platform/entitlements` | `stingops` 0005 (`DailyCounter`، `M0Snapshot`، `PlanEntitlement`، `OpsFlag`)؛ `stingops/growth.py` (`bump` من الصفحات العامة للسوق، `compute_m0`/`m0_payload` بختم زمني و`stale` بعد يوم، `entitlements_payload`، `set_entitlement` (403 `no_global_override`)، `set_flag` (400 `scope_required`)، `plan_feature_enabled` يسبق تعريف الباقة في `core.subscription.has_feature`)؛ `GET/POST /api/platform/m0`، `GET/POST /api/platform/entitlements`، `POST /api/platform/flags` | `features/platform/{m0-client.tsx (loading/ready/empty/stale), entitlements-client.tsx (ready/validation_error/permission_denied/success), platform-nav.tsx}`؛ `e2e/platform-m0-entitlements.spec.ts` (9/9)؛ `stingops/tests/test_operator.py` (6) | 0005 §٨٦ |
| T3.24 بوابة المرحلة ٣ | — | `POST /api/scenario/market` (`core/scenario/views.py` — بائع «ب» بانتظار التحقق وباقة «فرعان»، عرض عام وخاص بقائمة عضوها «أ»، مالك نشط للثالثة «ج»، مشغّل برمز جلسة)؛ `wipe_scenario` يمحو جداول السوق بترتيب المفاتيح المحمية | `e2e-gate/gate-market.spec.ts` (MK-4 → MK-1 → MK-2 + ACC-146/147 على Django الحقيقي؛ البوابة 14/14)؛ `docs/evidence/README.md` (صفوف MK-4/MK-1/MK-2/ACC-146…149 + خطوات §١٥.٥ اليدوية) | 0005 §٨٧ |
| T3.25 LINK-01 (5) + LINK-02 (5) — M3 | `/market/link/parties`، `/market/link/items` (خلف علم `market_m3`) | `market` 0014 (`MarketPartyLink`، `MarketItemMapping` + RLS)؛ `market/link.py` (`m3_enabled` من `OpsFlag`، `candidates` بلا «الأرجح»، `request_link`/`cancel_link`، `incoming_payload`/`decide_incoming` للطرف الآخر، `save_mapping` بـ`factor_required`/`needs_definition`، `confirm_mapping`، `mapping_for` للتحويل)؛ `GET/POST /api/market/link/parties[?q=]`، `POST …/parties/{id}/{request|cancel}`، `GET /api/market/link/incoming`، `POST …/incoming/{id}/{accept|reject}`، `GET/POST /api/market/link/items`، `POST …/items/{id}/{confirm|remove}` | `features/market/{link-parties-client.tsx (ready/validation_error/permission_denied/success), link-items-client.tsx (ready/validation_error/conflict/success)}` — بلا العلم تعرضان شاشة T3.17 `phase_locked`؛ `e2e/market-link-parties-items.spec.ts` (12/12)؛ `market/tests/test_link.py` (1) | 0005 §٨٩ |
| T3.26 LINK-03 (5) + LINK-04 (5) + LINK-05 (5) — M3 | `/market/link/receipts`، `/market/link/documents`، `/market/link/returns` (خلف علم `market_m3`) | `market` 0015 (`MarketDocumentLink` فريد لكل شحنة/مرتجع، `MarketShipmentDistinct` + RLS)؛ `market/link_docs.py` (`preview`، `convert` عبر `purchase_docs.approve` بمستند شراء معتمد بوحدة المورد ومعاملها — `409 duplicate_receipt` لاستلام يدوي مطابق، `attach_receipt_id`/`distinct_receipt_ids`، `unmapped_items`؛ `documents_payload` بالفروق وإثباتات الدفع المعلّقة، `settle` مساراً بلا تحريك رصيد؛ `returns_payload`، `convert_return` عبر `record_return`+`respond` بالمقبول وحده وحدّ `return_limits`)؛ `GET /api/market/link/receipts`، `GET …/receipts/{id}/preview`، `POST …/receipts/{id}/convert`، `GET /api/market/link/documents`، `POST …/documents/{id}/settle`، `GET /api/market/link/returns`، `POST …/returns/{id}/convert` | `features/market/{link-receipts-client.tsx (ready/validation_error/conflict/success), link-documents-client.tsx (ready/empty/conflict/permission_denied), link-returns-client.tsx (ready/partial/validation_error/success)}`؛ `e2e/market-link-docs.spec.ts` (18/18)؛ `market/tests/test_link_docs.py` (1) | 0005 §٩٠ |
| T3.27 بوابة MK-3 + GROW-01 (4) + GROW-02 (5) + GROW-03 (4) — M4 | `/grow/replenish`، `/grow/suppliers`، `/grow/promote` (خلف علم `market_m4`) | `inventory` 0011 (`GrowSnapshot` + RLS)، `market` 0016 (`MarketPromotionRequest` + RLS)؛ `inventory/grow.py` (`compute_replenish` من حركات البيع 30 يوماً + مهلة من المستندات + أمان 5 أيام، `replenish_payload` بصفوف لأمين المخزن والقيمة محجوبة، `forward_suggestion` إلى المالك، `order_from_selection` عبر `purchasing.create`، `compute_suppliers` ثلاثة أرقام بلا درجة وبعد 5 مستندات، `suppliers_payload` بـ`stale`)؛ `manage.py grow_compute` (ليلي — يحتاج مجدولاً في النشر)؛ `market/promotion.py` (`promote_payload` بقفل التسعير G-04، `preview` بوسم «عرض ممول» بجوار أرخص جارين، `save_request` save/request)؛ `GET/POST /api/grow/replenish`، `GET /api/grow/suppliers`، `GET/POST /api/grow/promote`؛ `wipe_scenario` يصفّر أعلام المنصة | `features/market/{grow-replenish-client.tsx (ready/empty/permission_denied), grow-suppliers-client.tsx (ready/loading/empty/stale), grow-promote-client.tsx (ready/validation_error/permission_denied)}`؛ `e2e/grow.spec.ts` (13 × 3)؛ `e2e-gate/gate-mk3.spec.ts` (MK-3 — 15/15)؛ `inventory/tests/test_grow.py` (1) | 0005 §٩١ |
| جولة UX/الهوية 2026-09-20/21 (#111) | `/` (هبوط)، `/register`، `/plans` (الباقات والمقارنة)، `/welcome`/`/login`/`/legal`/`/status` بترويسة عامة موحَّدة (الشروط والحالة بلوحة علوية وبطاقات)؛ كل الشاشات | `auth/account/register` (تذكرة `register` + كلمة مرور → تذكرة اختيار)؛ `public/contact` → `stingops.DemoRequest` (هجرة 0006)؛ `inventory._resolve_branch` أول فرع نشط للمالك بلا فرع جلسة؛ `tokens.json` لوحة `color.vezano` + `dark` | `packages/design/fonts` (Cairo/IBM Plex — OFL)؛ `theme.css` بالوضع الداكن؛ `ui-web`: Frame (`chrome`، `back`، شريط أفقي على الهاتف، `navLayout=top`)، Nav (`group`)، Table/LedgerLine mono على القيمة، Notice يلتفّ؛ web: `public-header.tsx` (يختفي مع النزول)، `landing.css`، `auth-aside.tsx`، `register-client.tsx`، `app-nav.tsx` مجمَّع، بطاقة الرمز في التطوير؛ `next.config` `distDir` من `NEXT_DIST_DIR`؛ `e2e/nav.ts`، `frame-provenance` يطبّع «فيزانو» | 0005 §٩٢–§٩٩ |
| T2.17 بوابة المرحلة ٢ | — | `campaigns.blockers` → `feature_unavailable` بـ`has_feature("campaigns")`؛ `dispatch` بترتيب حتمي؛ `POST /api/scenario/reset` يصفّر مفاتيح الأعطال | `e2e-gate/gate-subscription.spec.ts` (ACC-80/81، ACC-82)، `gate-public.spec.ts` (ACC-114، ACC-60)؛ تصلّب `notify-campaigns.spec.ts`/`parties.spec.ts` ضد سباق CI؛ `docs/evidence/README.md` 13 صفاً | 0005 §٦٣ |
| T1.16 POS-05 (5) + خط الحفظ | `/pos/pay` | نوع PUSH `sale` (Sale/SaleLine/Payment/StockMovement؛ الخادم يشتق الإجمالي والخصم وحركة المخزون — §٧.٣)؛ تطبيق `inventory` (`StockMovement` + `catalog.BALANCE_PROVIDERS` أرصدة الفرع)؛ إسقاطات `sales` تُطبَّق بترتيب أعضاء النوع (`sync/push.py`)؛ `sales.providers` يسجّل مزوّدي الوردية/المتأخر/الكتالوج/الأطراف/الخصم اليومي/الرئيسية (`sales_today`)/البحث (مستندات)؛ `branch_code` في `devices/register|renew` و`shifts/current` | `sync-core/sale-local.ts` (`saveSaleLocally` معاملة واحدة + رقم `INV-<فرع>-<جهاز>-<سنة>-<تسلسل>` + إسقاط + أرصدة + تفريغ السلة؛ `readSaleCashRows` لدرج SHIFT-02/03/04 وPOS-01)؛ `features/pos/pay-client.tsx`؛ `e2e/pos-pay.spec.ts` (يقرأ IndexedDB: مخزون −1، صندوق +100، لا ذمّة) |
| T1.15 POS-03 (3) + POS-04 (5) | `/pos/discount`، `/pos/customer` | تطبيق `parties` (`Party`، `GET/POST /api/parties` بـ409 `similar_party`، مرجعية `log_reference`، مُطبِّق `parties.PartyCreated`، سجلّا `BALANCE_PROVIDERS`/`LAST_SALE_PROVIDERS`)؛ تطبيق `sales` (`DiscountOverride` + مُطبِّق `sales.DiscountOverride`، `DISCOUNT_CAPS` G-09 مؤقتاً + `DISCOUNT_USAGE_PROVIDERS`)؛ `shifts/current` يعيد `role_code` و`discount_caps`؛ نوعا PUSH `party_create` و`discount_override` | `sync-core/parties-local.ts` (بحث/تشابه/إنشاء سريع محلي)؛ `pos-local.ts`: `CartDraft.discount/customer`، `cartTotals` بالخصم، `checkDiscount`، `requestDiscountOverride`؛ `shifts/context.ts` يحمل `discountCaps`؛ `features/pos/{discount-client,customer-client}.tsx`؛ `e2e/pos-discount-customer.spec.ts` |
| T1.14 POS-01 (6) + POS-02 (3) | `/pos` | `GET catalog/balances` من `catalog.services.BALANCE_PROVIDERS` (INV يسجّله) | `sync-core/pos-local.ts` (أرصدة `entity:inventory.Balance:*` بآخر مطابقة، صفوف صنف × وحدة، مسودّة السلة `pos.cart`/`pos.held`، `checkQty`)؛ `ui-web.Cart` بـ`onStep`/`summary`؛ `frame-text` يضمّ أسطح D37/D38؛ `features/pos/{pos-client,unit-sheet,empty-search,pos-nav}.tsx` + `lib/use-media.ts`؛ `e2e/pos.spec.ts` |
| T1.13 SHIFT-05 (5) | `/shifts/review` | `CashAdjustment` (§١٠.٣) + `ShiftCashMovement.received_at`؛ نوع PUSH `cash_adjustment` (نطاق الفرع) ومُطبِّقه؛ `LATE_DOCUMENT_PROVIDERS` (POS يسجّل البيع النقدي المتأخر)؛ `GET shifts/review` (المالك كل الفروع/غيره فرعه؛ ترتيب بالقيمة خادمي؛ مدى أسبوع؛ المهجورة ≥24س) و`POST shifts/{id}/review` (403 لغير المالك؛ السبب إلزامي مع فارق؛ `shift_open` للمفتوحة) | `sync-core.readPendingClosedShifts`؛ `tools/frame-text` يكتشف الرسمة الثانية للزوج (15-D10 لـSHIFT-05/conflict)؛ `features/shifts/review-client.tsx`؛ `e2e/shifts-review.spec.ts` |
| T1.12 SHIFT-03 (4) + SHIFT-04 (5) | `/shifts/movements`، `/shifts/close` | `sync/kinds.py`: `cash_movement` بثلاثة أنواع وعكس بالإشارة المضادّة وسبب إلزامي؛ `shift_close` (ShiftClosed + CashCounted بالفئات)؛ مُطبِّقات الإسقاط للعدّ والإقفال (اللقطة ثابتة؛ حركة متأخرة لا تعدّلها)؛ `shifts/{id}/requests` «اطلب من المالك»؛ `can_withdraw`/`owner_name` في `shifts/current` | `sync-core/shift-local.ts`: `saveCashMovement` (رقم محلي مثبَّت في الحدث، عكس)، `closeShiftLocally` (اللقطة + المعدود + إغلاق `shift.open`)؛ `features/shifts/movements-client.tsx`، `close-client.tsx` (المتوقَّع لا يُحسب ولا يُطلب قبل تأكيد العدّ)؛ `e2e/shifts-cash.spec.ts` |
| T1.11 SHIFT-01 (5) + SHIFT-02 (5) | `/shifts/open`، `/shifts/current` | تطبيق `shifts` (إسقاط `Shift`/`ShiftCashMovement` يُبنى داخل قبول PUSH عبر `sync/appliers.py`)، `shifts/current` و`shifts/{id}`، مزوّد الرئيسية `shift`، `CASH_EFFECT_PROVIDERS` لـPOS/PTY | `sync-core/shift-local.ts` (فتح محلي = عملية `shift_open` + إسقاط + meta `shift.open`؛ صفوف الوردية والمتوقَّع من المجال) + اختبار Dexie؛ `lib/sync.ts` ناقل الرفع المشترك؛ `features/shifts/*`؛ `e2e/shifts.spec.ts` (IndexedDB حقيقي) |
| T1.10 CAT-04 (4) + CAT-05 (6) | `/catalog/[id]/price`، `/catalog/import` | `catalog/prices.py`: `ItemPrice` (سلسلة تواريخ؛ هجرة سطر أول لكل صنف)، `PriceChangeRequest`، `PriceImportBatch` (بصمة الملف = هوية؛ تطبيق على دفعات واستئناف وتراجع 24س)؛ نقاط `items/{id}/price`، `price-request`، `prices/import/preview|{b}|apply|revert|rejected.csv`؛ سجلّات `COST_PROVIDERS`/`PRICE_USAGE_PROVIDERS`/`BULK_PRICING_BLOCKERS` | `features/catalog/price-client.tsx` + `import-client.tsx`؛ `e2e/catalog-prices.spec.ts` |
| T1.9 CAT-02 (4) + CAT-03 (3) | `/catalog/new`، `/catalog/[id]`، `/catalog/[id]/units` | `catalog/limits.py` (ACC-25: رفض بكل الأخطاء معاً)، `Unit.decimal_places`، `Item.image_data_url`، `ItemUnit.barcode`، `ItemUnitFactorChange` (ACC-19)، نقاط `catalog/units|barcode|items/{id}[/image|/units[/{iu}]]`، سجلّا مزوّدين `FACTOR_USAGE_PROVIDERS`/`ITEM_MOVEMENT_PROVIDERS` | `domain.fromBaseQtyMilliForDisplay` بمتجه؛ `features/catalog/item-form.tsx` + `item-errors.ts` + `units-client.tsx`؛ `e2e/catalog-item.spec.ts` |

---

## 3 · الأدوات والأوامر التي تعمل

### التثبيت

```bash
cd /Users/macbookairm1/Documents/vezona-business
git fetch --all && git checkout phase0/t0.20-scenario-seed   # أو main إن دُمجت السلسلة
pnpm install
cd backend && uv sync && uv run python manage.py migrate
STING_ENV=development STING_FAULTS_ENABLED=1 uv run python manage.py scenario reset
cd ../apps/web && pnpm exec playwright install chromium   # مرة واحدة
```

المتطلبات الموجودة على الجهاز: Node 26 (`.nvmrc`=24، engines ≥24)، pnpm 12، Python 3.12 عبر uv 0.12، PostgreSQL 16 (Homebrew، يعمل؛ قاعدة التطوير `sting_dev`).

### الفحص الكامل (يجب أن يخضرّ قبل أي PR)

```bash
pnpm check                      # lint + typecheck + test + boundaries
```
```bash
cd backend && STING_ENV=test STING_FAULTS_ENABLED=1 uv run pytest -q && uv run ruff check . && uv run mypy .
```
```bash
cd apps/web && pnpm exec playwright test
```

### أوامر يُنسى أنها مطلوبة

- **بعد أي تغيير في DRF view/serializer**: `pnpm --filter @sting/contracts generate` — وإلا يفشل اختبار الانحراف.
- **بعد أي تغيير في `tokens.json`**: `cd packages/design && node scripts/generate.ts`.
- **Storybook**: `pnpm --filter @sting/ui-web storybook` / `build-storybook`.
- **استخراج نصوص إطار**: من `tools/frame-text` — `frameTexts("ACC-01","ready")`، `frameTextsAll()` (كل إطارات الزوج)، `drawnStates("ACC-01")`.
- **e2e بلا Django**: الخادم يُحاكى بـ`page.route("**/api/…")` وفق أشكال `@sting/contracts`؛ منطق الخادم في pytest. البوابة T1.43 تشغّل خادماً حقيقياً.
- **بطاقات الحالة (34-D26 وأمثالها)**: عنوان البطاقة = عنوان الحالة، العبارات بين «…» = نصوص واجهة، المبرِّرات لا تُعرض (0005 §١). `fromFrame()` يقبل النص من أي إطار مرسوم للشاشة نفسها.
- **الجلسة في الذاكرة فقط**: اختبارات e2e تنتقل عميلياً (`AppNav`، `?next=` في الدخول) — `page.goto` بعد الدخول يفقد الجلسة. `devIndicators` معطّل في next.config (الزر العائم كان يعترض النقر في 390).
- **مزوّدو الرئيسية والبحث**: كل وحدة جديدة (POS/PTY/INV) تسجّل مزوّدها في `core/home.py` ومحلّل مرجعياتها في `sync/reference.py` (انظر `catalog/providers.py` نموذجاً).
- **المرجعيات الخادمية** (كتالوج/أطراف): كل كتابة تمرّ بـ`log_reference()` داخل معاملة وإلا لا تصل الأجهزة.
- **الرفض المضبوط (ACC-25)**: أخطاء النماذج الخادمية `400 {detail:"validation_error", errors:[{field,code,limit,actual}]}` بكل الأخطاء معاً؛ الواجهة تصوغ النص من `features/catalog/item-errors.ts` (نصوص الحدود مؤقتة حتى تُرسم — 0005 §١٢).
- **مزوّدو الاستعمال**: POS تسجّل `catalog.services.FACTOR_USAGE_PROVIDERS` (سطور البيع بمعامل وحدة) و`catalog.prices.PRICE_USAGE_PROVIDERS` (بيع بسعر — يمنع تراجع الدفعة)؛ INV/POS `ITEM_MOVEMENT_PROVIDERS` (قفل الوحدة الأساسية)؛ INV/PUR `catalog.prices.COST_PROVIDERS` (متوسط التكلفة — «سعر دون التكلفة»)؛ ORG `BULK_PRICING_BLOCKERS` (§١١.٢) — حتى ذلك الحين صفر/فارغ.
- **إسقاطات الخادم من PUSH**: كل وحدة تسجّل `sync.appliers.register_applier(entity, fn)` — يُستدعى داخل معاملة القبول؛ متكرّر الأثر. الحفظ المحلي: مُسقِط `saveOperation` لا يستدعي دالة async مساعدة تحت Dexie (PrematureCommitError) — كل await على المعاملة مباشرةً.
- **الرفع من الشاشات**: `lib/sync.ts::pushPending()` (ناقل مشترك) بعد كل حفظ محلي حين يوجد اتصال.
- **كل كتابة سعر** تمرّ بـ`catalog.services.record_price` (سطر في `ItemPrice` + إغلاق الساري) — لا تكتب `Item.sale_price_minor` مباشرة.
- **pytest `testpaths`** في `backend/pyproject.toml` يجب أن يضم كل تطبيق جديد (كانت `catalog` خارجه).
- **مجموعة الفحص الكاملة تتجاوز 10 دقائق**: شغّل Playwright في الخلفية أو ملفاً ملفاً.
- **رسمة ثانية للزوج نفسه** (قسم `id="SCREEN"` في ملف آخر يحمل سطر المرجع `P/SCREEN/VP/state`): `frameTextsAll` يكتشفها تلقائياً فتدخل نصوصها في `fromFrame` (15-D10 لـSHIFT-05). سجّلها في 0005 حين تعتمد عليها.
- **أفعال غير متزامنة في الاختبار** (طباعة بعد فحص `printer_fail`، رفع بعد حفظ): عدّادات `window.print` المطعّمة تُفحص بـ`expect.poll` لا بـ`expect(await …)` — الضغطة تعود قبل اكتمال `fetch`. الحالة `success` في POS-08 = `synced` + طبعة في هذه الجلسة (ذاكرة المكوّن، لا Dexie).
- **الأرقام في بيانات السكربت** (`'22,440.00'`) لا يستخرجها `frame-text` — تُفحص بـ`toContainText` خارج `fromFrame`.
- **فتح صف الجدول** (`Table.onOpenRow`) نقر مزدوج/Enter فقط — للّمس ضع زرّاً في الخلية (`Button variant="quiet"`).
- **`responses={200: None}`** في DRF يولّد `content?: never` فيصير `data` من `openapi-fetch` من نوع `never`: اكتب `const body: Shape | undefined = data` (لا `as` — eslint يرفضه) للـGET، و`as unknown as` للـPOST.
- **نوع PUSH جديد** يحتاج ثلاثة مواضع: `sync/kinds.py` (register)، `sync/scopes.py` (`ENTITY_SCOPES`)، ومُطبِّقه في `providers.py` للوحدة — نسيان النطاق يرمي `has no scope mapping`.
- **إسقاطات بتوقيت متساوٍ** (`occurred_at` نفسه) ترتيبها بالـuuid4 عشوائي — الاختبارات تعطي أوقاتاً مختلفة.
- **أسطح D37/D38** (تابلت POS 834…) غير موسومة في المصفوفة: `frameTextsAll` يضمّ قسم S-nn الذي يذكر الشاشة، فتُقبل نصوصه في `fromFrame`؛ نصوص `renderVals()` في 03-D2 (بيانات الأصناف) لا تُستخرج — تُفحص بـ`toContainText`.
- **`page.reload()` يُفقد الجلسة** (في الذاكرة فقط) — للتحقق من المسودّات اقرأ IndexedDB بـ`page.evaluate`.
- **axe**: رأس جدول فارغ مرفوض (`empty-table-header`)، ومستوى العناوين يجب ألا يقفز (h2 قبل h3).
- **POS على التابلت**: `useMedia("(max-width: 1199px)")` يبدّل الجدول إلى بلاطات؛ الاختبار يفرّع على `page.viewportSize()`.
- **جلسة أخرى تشغّل pytest على `sting_dev`** تُعلّق تشغيلك (قاعدة الاختبار `test_sting_dev` مشتركة): `createdb sting_t1XX` ثم `DATABASE_URL=postgresql:///sting_t1XX` قبل pytest.
- **«٪» حرف عربي** (U+066A) — خارج `.sting-mono` دائماً؛ تطبيق Django جديد يحتاج: `INSTALLED_APPS`، `sting/urls.py`، `pyproject testpaths`، `sync/scopes.py`، هجرة بـRLS.
- **سجلّات المزوّدين تُملأ عند تحميل التطبيقات** (`sales.providers` يسجّل في shifts/catalog/parties): اختبارات الوحدات تحفظ القائمة وتعيدها (`saved = list(X); X.clear(); … X[:] = saved`) — `clear()` وحده يُسقط مزوّدي POS لبقية الجلسة.
- **مُطبِّقات PUSH تُستدعى بترتيب أعضاء النوع في `KindSpec.members`** (الرأس قبل سطوره) — لا تعتمد على الترتيب الأبجدي للكيانات.
- **`saving` مرئي في Playwright** حين تبقى الحالة حتى محاولة الرفع الأولى؛ افحص تعطيل الزر قبل `expectFrame` (اللقطة وaxe تستهلك ثوانٍ).
- **اختبارات POS-06/07 تبدأ بتسجيل دخول لكل حالة** — أول اختبارين على 390 قد يسقطان بمهلة تحت حمل خارجي (جلسة أخرى)؛ أعد الملف وحده قبل الحكم.

### حلقة بناء الشاشة (تُكرَّر لكل شاشة في المرحلة ١)

1. `drawnStates(screenId)` → قائمة الحالات المرسومة وملفاتها.
2. لكل حالة: `frameTexts(screenId, state)` → النصوص الحرفية (`staticTexts` + `scriptTexts`)؛ افتح ملف `dc.html` المذكور واقرأ الإطار نفسه لتعرف التخطيط والمكوّنات.
3. ابنِ الصفحة في `apps/web/src/app/…` من مكوّنات `@sting/ui-web` فقط؛ الجذر يحمل `data-screen="ACC-01" data-state="…"`.
4. اختبار `apps/web/e2e/<screen>.spec.ts` على نمط `probe.spec.ts`: `expectFrame(page, info, {screenId, state, texts, styles})` يمرّ على المقاسات الثلاثة.
5. نقاط نهاية DRF + اختباراتها + تجديد العقود.
6. commit + PR مكدّس على الفرع السابق (`gh pr create --base <previous-branch>`)، ثم تقرير: ما بُني / ما اختُبر / ما بقي — **بلا تقديرات زمنية غير مقاسة**.

### أعطال معروفة وحلولها

| العطل | الحل |
|---|---|
| Prettier يعيد تشكيل النص فتفشل التعديلات النصية الدقيقة | شغّل `pnpm format` قبل أي تعديل مبرمج، أو اكتب الملف كاملاً |
| صنّف الأذونات يمنع `git add -A && git commit` المتسلسلة و`gh pr merge` | قسّم الأوامر؛ الدمج للمالك |
| `timeout` غير موجود على macOS | لا تستخدمه |
| منتظر الدمج `until s=$(gh pr checks N)` يدور إلى الأبد على PR أحمر (`gh pr checks` يخرج بغير صفر عند أي فشل) | استعمل `gh pr checks N --json name,bucket` وافحص الـbuckets (النمط في ذاكرة الجلسة: merge-chain.sh) |
| `Notice` بـ`action` على 390: جسم الإشعار ينكمش إلى عرض صفر فيظهر العنوان «hidden» لـPlaywright | ضع الزر داخل الأبناء في `acc-actions` بدل `action` |
| `useState` بعد `return` مبكر (`phase_locked`) → خطأ ترتيب الخطافات وشاشة بيضاء في المواصفات | كل الخطافات قبل أي `return` شرطي |
| البوابة تسجّل جهازين فوق جهازي البذر؛ حدّ التجريبية 3 أجهزة (ORG-06) ردّ الثاني بـ`device_limit` فظهر ACC-05 `server_error` في اختبارات الجهازين فقط | منشأة السيناريو تُبذر على باقة «فرعان» (6 أجهزة) في `seed_scenario`؛ أي حدّ جديد للباقة يُراجَع مقابل البذر |
| نموذج `TenantScoped` جديد يُسقط `/api/scenario/reset` في البوابة بـ`ProtectedError` | `wipe_scenario` يكنس الآن كل `TenantScoped` قبل حذف المستأجر؛ ما يشير إلى فرع/مستخدم بـPROTECT يحتاج ترتيباً صريحاً قبله |
| StrictMode في `next dev` يكرّر طلب التحميل الأول فتنتهي حالة `loading` قبل إثباتها | في الاختبار احجز الاستجابة بوعد يُحرَّر بعد `expectFrame(loading)` بعلم `released` (لا بعدّ الطلبات) |
| تشغيل pytest ثانٍ على `DATABASE_URL` نفسه أثناء تشغيل آخر يعطي ERRORs زائفة | pytest واحد في كل مرة |
| `@theme{}` يُهمَل بلا Tailwind | المولّد يصدر نسخة `:root{}`؛ Tailwind غير مفعّل في `apps/web` بعد (قرار مؤجَّل) |
| `next build` يفشل لغياب ملفات الخطوط | `apps/web/scripts/prepare-fonts.mjs` يولّد `fonts.generated.css` فقط إن وُجدت الملفات |
| `exactOptionalPropertyTypes` | الخصائص الاختيارية تُعلن `?: T \| undefined` |
| jsx-a11y يرفض مستمعي لوحة المفاتيح على عناصر ثابتة | الاختصارات (F6/Escape) على `document` |
| قاعدة «لا عربية داخل mono» | التواريخ والإعلانات المخفية خارج خلايا mono |
| `openapi-fetch` يضع جسم الردّ 4xx في `error` لا `data` | اقرأ رموز الخادم من `error` (`const { data, error, response } = await api().POST(...)`) — صُحّح في شاشات T1.26–T1.32 |
| تحت Dexie الحقيقي (Chrome) أي `await` لمساعد `async` داخل `storage.transaction` يرمي `PrematureCommitError` — حتى لو لم يَعُد إلا على `tx.*`؛ وحتى مساعد `.then` غير async يسقط حين يتكرّر في حلقة (دفعة من عمليتين فأكثر) | اكتب على `tx` مباشرةً داخل المُسقِط: `tx.putMeta(k, appended(await tx.getMeta(k), entry))` — مساعدات نقية فقط بين استدعاءات `tx.*`. Vitest (fake-indexeddb) لا يكشفه — Playwright يكشفه |
| التشغيل الكامل لـPlaywright يقلع `next dev` على شجرة العمل: تعديل الشيفرة أثناءه (sync-core، `lib/sync.ts`) يدخل الحزمة بالتحميل الحيّ ويلوّث النتيجة | لا تعدّل الشيفرة أثناء التشغيل الكامل — اكتب وثائق/اختبارات المهمة التالية، أو اعتمد Playwright في CI على الالتزام المدفوع |
| ESLint يحلّل `apps/web/public/*.js` (عامل الخدمة) ويسقط بـ«not found by the project service» | أضف الملف إلى `ignores` في `eslint.config.js` (كما `apps/web/public/sw.js`) |
| في Playwright بعد `seed()` (يعيد التحميل) ثم دخول سريع قد يصل النقر قبل ترطيب React فيضيع | انتظر حالة الشاشة (`expectFrame`/`toHaveAttribute(data-state)`) قبل أي نقر بعد إعادة التحميل؛ ولا تفترض الرفع بعد الحفظ — `expect.poll` |
| معرّفات UUID7 المنشأة في الملّي ثانية نفسها لا تحفظ ترتيب الإدخال | سطور المستندات تحمل `line_no` ويُرتَّب بها (`StockOpeningLine`، `StockTransferLine`)؛ لا تعتمد على `order_by("id")` للترتيب الزمني داخل معاملة |
| اختبارات الوقت («منذ N يوماً»، «أمس 16:20») تسقط بعد منتصف الليل أو باختلاف المنطقة الزمنية | احسب بالأيام التقويمية (`Since` في PTY-05)، وابنِ أوقات الاختبار بالتوقيت المحلي (`setHours`) لا بـ`toISOString().replace` |
| اختبارات pytest تسقط في CI بين 00:00 و08:00 UTC فقط (وردية «أمس 08:00Z» لم تبلغ 24 ساعة؛ `t0 = now − 10 دقائق` يقع في يوم UTC السابق فيخرج من نطاق «اليوم») | ثبّت الأزمنة صراحة (`occurred_at = now − 30h`) أو اطلب نطاقاً أوسع (`?range=week`) — أُصلح في `test_reports_stock_cash` و`test_duplicates` (T3.19)؛ لا تعتمد على `D = أمس` لشرط مدة |
| مواصفات Playwright تسقط في CI بعد منتصف الليل UTC («أمس» بحساب 24 ساعة، مهلة `now+20h` تصير «اليوم») | `whenWord` بالأيام التقويمية المحلية؛ ثبّت المهل على «الغد 10:00» (`tomorrowAt10`) — أُصلح في T3.20 لـCUS-01/03 وORD-04 |
| `uv run pytest` بلا وسائط يقرأ `testpaths` في `backend/pyproject.toml` — أي تطبيق Django جديد لا يُختبر في CI حتى يُضاف هناك (كانت `market` و`stingops` خارجه حتى T3.20) | أضف اسم التطبيق إلى `testpaths` مع أول اختبار له، وتحقّق بـ`uv run pytest --co \| tail -1` أن العدد شمله |
| خادم البوابة المحلي (`runserver 8100 --noreload` مع `reuseExistingServer`) يبقى على شيفرة قديمة بين التشغيلات | بعد أي تعديل خادمي: `lsof -ti tcp:8100 \| xargs kill` ثم أعد `pnpm --filter @sting/web e2e:gate` |
| CI يشغّل pytest بـ`STING_ENV=ci` ومحلياً `test` — اختبار يقارن اسم البيئة حرفياً يسقط في CI وحده | اقرأ `os.environ["STING_ENV"]` في الاختبار (أُصلح في T3.24 لاختبار الاستعادة الحيّة) |
| `POST /api/scenario/reset` يسقط بـ`ProtectedError` عند أي جدول جديد بمفاتيح محمية (طلبات السوق وأبناؤها) | أضف الجدول إلى كتلة الحذف المرتّبة في `wipe_scenario` **قبل** الكنس العام لـ`TenantScoped` |
| مهمة `web` في CI تسقط أحياناً بـ`ERR_CONNECTION_REFUSED`/`ERR_EMPTY_RESPONSE` على `localhost:3000` (خادم Next التطويري ينهار تحت 1400 اختبار) | ليست عيباً في الشيفرة: `gh run rerun <run-id> --failed`؛ الفاشل الحقيقي يظهر بـ«نصوص حرفية ناقصة» أو `toHaveAttribute` |
| المستخرج لا يلتقط نصوص `placeholder` ولا بطاقة خارج عنصر مرجع الإطار (كـ`saved_local` في 28-D21 INV-04) | افحصها بـ`toContainText` وسجّل ذلك في الملحق |
| الجلسة في الذاكرة فقط: `page.reload()` يعيد إلى الدخول | اختبر الاستئناف بالتنقّل داخل التطبيق (زرّ ثم عودة) لا بإعادة التحميل |
| مصادقة الكلاسات في `spectacular`: وجهة `POST` بلا جسم تحتاج `@extend_schema(request=None)` وإلا «unable to guess serializer» | أضفها لكل `APIView.post` بلا serializer |
| اسم `Serializer` داخلي متكرر بين تطبيقين (مثل `OpeningSerializer`) يُدمَج في `openapi.json` فتنكسر أنواع العميل | سمِّ المسلسِلات الداخلية باسم فريد (`StockOpeningSerializer`) |
| البوابة (`e2e-gate`): `/api/scenario/reset` يرفض بـ`production_guard` | اسم القاعدة يحمل وسم اختبار: `sting_gate_test` |
| البوابة: `context.setOffline` يُسقط التنقّل كله (خادم Next التطويري يخدم أجزاء المسارات عند الطلب) | اقطع الشبكة بإسقاط `/api/*` + حدث `offline` (`cutNetwork` في `e2e-gate/fixtures.ts`) |
| البوابة: Chromium بلا رأس لا يمنح `navigator.storage.persist()` أبداً (ولا عبر CDP) فيقف التجهيز على «صلاحية مرفوضة» | الوسيط يحاكي المنح في `newDevice`؛ المنح الحقيقي يُتحقق يدوياً على الجهاز |
| البوابة: مساعد شاشة يقرأ القيمة المخزَّنة من زيارة سابقة قبل وصول ردّ الخادم | `page.waitForResponse(...)` قبل قراءة الشاشة (`stockScreen`/`shiftScreen`/`statement`) |
| RLS تُنفَّذ على CI (دور `sting`) لا محلياً (superuser يتجاوزها) — سلوك يمرّ محلياً ويسقط على CI | اختبر منطق العزل على CI أو بدور غير superuser؛ لا تفترض تطابق المحلي |
| إعادة تشغيل أثر التجهيز (`use-bootstrap.ts`) أثناء تسجيل جارٍ كانت تسجّل جهازين | وعد تسجيل مشترك `deviceOnce` — لا تُعد إدخال تسجيل متزامن |
| اختبارات pytest بتواريخ ثابتة داخل نوافذ زمنية (نافذة مراجعة 7 أيام) تسقط بمرور التقويم | تواريخ نسبية («أمس/اليوم») كما في `shifts/tests/test_shifts.py` |
| تشغيل `tsc` بعد تبديل فرع يشكو صفحة لم تعد موجودة (`.next/types` قديمة) | `rm -rf apps/web/.next` |
| منفذ 3000 مشغول بمشروع آخر على الجهاز (صفحة غريبة على `localhost:3000` فتفشل كل المواصفات عند «رقم الهاتف أو البريد») | لا تقتل العملية — `PLAYWRIGHT_PORT=3011 pnpm exec playwright test …` (المتغيّر في `playwright.config.ts`؛ CI على 3000)؛ تحقّق بـ`lsof -i :3000` |
| فشل جماعي في وظيفة `web` على CI (ERR_CONNECTION_REFUSED في منتصف التشغيل + اختبارات لا صلة بينها) | خادم التطوير سقط — `gh run rerun <run-id> --failed` قبل لمس الكود، ثم أعد تشغيل `merge-chain.sh` لأنه يخرج عند أي `fail` |

---

## 4 · ما ينتظر المالك (لا يستطيع المنفّذ فعله)

| # | البند | يوقف ماذا |
|---|---|---|
| 1 | **دمج السلسلة #1 → #21 بالترتيب** | لا يوقف البدء (يمكن التكديس فوق #21) لكنه يُبسّط كل ما بعده |
| 2 | **إذن تنزيل 8 ملفات خطوط OFL** إلى `packages/design/fonts/` (القائمة في `packages/design/fonts/README.md`: Cairo[slnt,wght].ttf، IBMPlexSansArabic-{Regular,Medium,SemiBold,Bold}.ttf، IBMPlexMono-{Regular,Medium,SemiBold}.ttf) | مطابقة الأنماط المحسوبة للخط في Playwright؛ حتى ذلك الحين يعمل التطبيق بخط النظام |
| 3 | **قرار 0005 §٣**: أين تُجمع بيانات الحساب (identifier + كلمة مرور) عند «إنشاء منشأة جديدة»؟ وما شاشة كلمة المرور الجديدة بعد رمز الاستعادة؟ | ACC-04 ومسار الاستعادة (مطبَّقان بافتراض) |
| 3b | **تعارضات مسجّلة تنتظر حسمك** في 0005: عمر رمز التحقق 5/10 دقائق (§٢)، قفل PIN 5/10 محاولات (§٧)، قائمة خطوات المعالج D2/D26 (§٩)، الأدوار المؤقتة حتى G-09 (§١٠) | لا يوقف شيئاً — القيم في مكان واحد لكل منها |
| 4 | ~~جواب س٤~~ **حُسم 2026-09-18**: النسخة المحلية بكلمة حماية + AES‑GCM (0002 س٤، 0005 §٤٠) والترقيم على `02-Design-System`؛ وحُسم أيضاً أن الاستعادة دمجٌ بالهويات لا إحلال (0005 §٤٠) | — |
| 5 | Expo/Tauri (المرحلة ٤) | لا تبدأ إلا بموافقة كتابية بعد التجربة الميدانية (§١٥.١ مرحلة د) |
| 6 | **افتراضات PTY/INV تنتظر حسمك** في 0005 §٢٩–§٣٥: الفترة المقفلة = قبل أول الشهر السابق (§٢٩)؛ حدّ التصدير 500 سطر و40 سطراً للصفحة (§٣٠)؛ حد التنبيه عبر API فقط (§٣١)؛ أزرار الاعتماد/الإرسال غير المرسومة (§٣٢)؛ الأثر المالي للجرد والهالك والطريق بسعر البيع حتى تُبنى التكلفة (§٣٣–§٣٥)؛ حدّ الهالك المالي 500.00 لغير المالك حتى ORG-02 (§٣٤)؛ رقم `TS-NNNN`/`DMG-NNNN` (§٣٣–§٣٤) | لا يوقف البناء — كلها معلَنة في الشاشات |
| 7 | **افتراضات SYS/WEB/ORG/REP/NOT/PUR تنتظر حسمك** في 0005 §٣٧–§٩٠: `stale` بعد ساعة (§٣٧)؛ لا قناة رجوع لقرار المراجعة إلى الجهاز (§٣٨)؛ أيام المساحة تقدير خشن (§٣٩)؛ تفرّد رقم الفاتورة غير مفروض خادمياً (§٣٧)؛ **أيقونات `manifest.webmanifest` 192/512 من ملفات التصميم — التثبيت على Android يحتاجها (§٤٣)**؛ **مفاتيح VAPID في بيئة النشر (`vapid --gen`) ونصوص أزرار WEB-02 غير المرسومة (§٤٤)**؛ **عتاد الطباعة: طرازان BLE على جهازين لإثبات ACC-83 وتعبئة `docs/evidence/printers.md` و`models.json` → `tested` (§٤٥)**؛ **البوابة (§٤٦): وجهات مؤشرات الرئيسية، والتحقق اليدوي من منح التخزين الدائم على الجهاز المستهدف، وACC-72 على PWA مثبَّت، وACC-63/105/109 تنتظر المرحلة ٢؛ **ORG-02 (§٤٧): سقوف الخصم من المصفوفة بقيم الإطار (200/10 للعملية) بدل الثوابت السابقة، والحدّ اليومي والنسبة ثابتان حتى يُحرَّر مدى يومي، ولوحة «الإجراء وأثره» نصّ ثابت؛ **ORG-03/04 (§٤٨): عتبات الاتصال 15 دقيقة/3 أيام، رمز الفرع 2–6 لاتيني، حدّ الباقة يأتي مع ORG-06؛ **ORG-05 (§٤٩): الإقفال الإداري لوردية المعطَّل غير مبني فيبقى مانعاً، رسالة «سُحب وصولك» المخصّصة غير مرسومة، والمعلّق يُعرض عدداً لا محتوى؛ **ORG-06/08 (§٥٠): الباقات وأسعارها وحدودها بقيم الإطار (فرع واحد 45,000 / فرعان 85,000 / تجريبية 30 يوماً) بدل أساسي/نمو/سلسلة في §١٦.١، والمنشآت السابقة تُبذر تجريبية 30 يوماً من إنشائها؛ **ORG-07 (§٥١): الصورة في الصف base64 حتى مخزن ملفات، المراجعة عبر API حتى PLT-03، «يوم عمل واحد» نصّ معلن لا قياس؛ **ORG-09/10 (§٥٢): عرض الورق 32/48 حرفاً تقدير، نمط الأرقام على المعاينة فقط حتى REP/PRT، «تعديل متزامن» نصّ غير مرسوم تحت `conflict`، سجل التدقيق لا يستعيد ما قبل الهجرة؛ **REP-01/02 (§٥٣): قيمة المعلّق التقديرية لا تُعرض (الخادم لا يعرفها)، الفاتورة المختلطة تُحسب كاملة تحت كل وسيلة فيها، «أقدم حركة غير مسددة» تعريف تقريبي معلَن، الذمم بلا فلتر فرع؛ **REP-03/04 (§٥٤): الإقفال الثاني للوردية يُحجر بدل الكتابة فوق الأول وقبوله في SYS-03 يوثّق ولا يستبدل العدّ، تقرير المخزون لفرع واحد لا مجموع مواقع، الكاشير يُعاد إلى INV-01/SHIFT-02 بلا حالة منع مرسومة؛ **REP-05/06 (§٥٥): سياسة التكلفة عبر الـAPI فقط (آخر شراء/متوسط مرجَّح؛ FIFO غير مبني)، الهامش يمتنع كلياً عند أي صنف بلا تكلفة، حصة التصدير الشهرية بالباقة تقدير، «اطلب مدى أوسع» يسجّل ولا يرفع الحدّ؛ **NOT-01/02 (§٥٦): صلاحية الرابط 7 أيام، البريد في التفضيل لا الحساب، رصيد الرسائل النصية بالباقة بلا مُرسِل بعد، طلب السوق والقيمة التقديرية للمعلّق غير مبنيين؛ **NOT-03/04 (§٥٧): أجزاء الرسالة 160/153 حتى يُعتمد مزوّد الرسائل (العربية 70/67)، لا مسار عام للاشتراك عبر الرابط/QR بعد، متابعو السوق صفر حتى السوق؛ **NOT-05/06 (§٥٨): مزوّد الرسائل محاكاة خلف مفاتيح الأعطال حتى يُعتمد مزوّد وعقده، عامل الجدولة كسول عند الجلب لا خلفي، إذن الزبون عبر مساعد السيناريو حتى يُبنى رابط الاشتراك/QR في PUB؛ **PUR-01/02 (§٥٩): لا قناة إرسال للمورد بعد (يُعلَّم مُرسلاً)، أرقام الأوامر بلا بادئة كما في الإطار، صلاحيات الشراء خارج مصفوفة ORG-02، القيمة التقديرية من آخر شراء لا من سياسة التكلفة؛ **PUR-03/04 (§٦٠): استحقاق ذمّة المورد 30 يوماً ثابت، المتوسط المرجّح في أثر الاعتماد بمعزل عن `cost_policy`، مستند بلا أمر والمستند المضادّ وإعادة المرفوض للمخزون غير مبنية، حدّ المدير 3,000 بقيمة الإطار؛ **PUR-05 (§٦١): المتوسط المرجّح افتراضاً بلا سياسة (REP-05 يبقى مقفلاً حتى السياسة)، مفتاح `purchasing_enabled` لا يقفل PUR-01…04، التكلفة اليدوية في الإعدادات لا في `sales.margin`؛ **PUB-01…04 (§٦٢): النصّ القانوني موقوف على G-11 (مراجعة قانونية)، صفحة الحالة تحتاج مضيفاً مستقلاً ومسؤول نشر (§١١.٩) — المبني يقرأ الخادم ويحفظ آخر لقطة، إعلان الصيانة وسجلّ الأحداث من متغيّرات البيئة، لا SSG فعلي بعد؛ **البوابة (§٦٣): إيقاف التقارير التحليلية والاستيراد بعد المهلة غير مفروض خادمياً (الحملات وحدها) — يُوصل إن أردته الآن؛ **CUS-01/02 (§٦٤): `slug` عشوائي، العنوان والساعات عبر الـAPI حتى MP-09، `valid_until` للحملة عبر الـAPI حتى يُرسم، Push للمشتركين ينتظر مفاتيح VAPID؛ **CUS-03…05 (§٦٥): صندوق الوارد قناة قابلة للإيقاف، لا بريد للزبون (نصّ الإطار يذكره)، الرسالة النصية للمشترك بلا مزوّد؛ **MP-08/09 (§٦٦): مراجعة التحقق عبر الـAPI حتى PLT-06، «3 أيام» مدة معتادة ثابتة، القوائم مفصولة بـ«·»، صفحة السوق وبوابة الزبون كيانان منفصلان؛ **MP-10/11 (§٦٧): صلاحية العرض 14 يوماً افتراضاً، `market_publish` للمالك وحده، القائمة الخاصة عبر الـAPI حتى MP-12؛ **MP-12/13 (§٦٨): سعر الوحدة بقسمة صحيحة، الدعوة بمعرّف يُلصق حتى MP-02/06، التجديد 48 ساعة، إخطار المشترين بتغيّر التوصيل ينتظر ORD/NOT؛ **MP-01/02 (§٦٩): مطابقة المنطقة بالاحتواء النصي، اللقطة المحلية لا تُصفّى بتاريخها، «رخصة تحتاج تجديداً»/«نشر معلَّق» تنتظران PLT؛ **MP-03/04 (§٧٠): الشارة 365 يوماً، الحقائق صفر حتى ORD، المنتهي للسياق أسبوعاً، حقول الرسوم عبر الـAPI حتى تُدرج في MP-11؛ **MP-05/06 (§٧١): السلة المحلية `market.cart` حتى ORD-01، «طلب تأكيد من المورد» يقود إلى ملفه (لا قناة رسائل بعد)، المتابعة للمالك وحده، أسماء العملات قاموس ثابت؛ **MP-07/14/15 (§٧٢): الرابط 7 أيام، المشاركة والدعوة وطلب التخويل بصلاحية `market_publish`، قبول التخويل لا يُدرج في قائمة خاصة تلقائياً، الدليل base64 في الصف، قرار البلاغ عبر الخدمة حتى PLT-07، «انتهت الصلاحية» بلا ساعة، أرشفة عرض داخل طلبات قائمة تنتظر ORD؛ **ORD-01/02 (§٧٣): السلة محلية `market.cart`، حدّ القسم أعلى حدّ بين بنوده، مهلة الرد 72 ساعة، الإرسال بصلاحية `purchase_approve` وسقفها، «التسليم إلى» نصّ حتى ربط الفروع، الصلاحية بتاريخ بلا ساعة؛ **ORD-03/04 (§٧٤): إعادة الإرسال على الطلب نفسه بإصدار جديد، «مهلة قاربت» 6 ساعات، رقم واحد `PO-n` عند الطرفين، الفلتر «قائمة» يستثني محل الخلاف والملغى بإعلان؛ **ORD-05/06 (§٧٥): صلاحية العرض 3 أيام افتراضاً، التعديل بعد القبول لا يغيّر الحالة، قيمتا المستلم/الفارق مجموعان لا قيد دفتري (LINK-03 مقفل)، العرض بصلاحية `market_publish`؛ **ORD-07/08 (§٧٦): صلاحية العرض حتى نهاية يومها، `requote` يعيد الطلب إلى الانتظار، «سُلِّم» = اكتمل الشحن، الشحن بصلاحية النشر، `delivery_days` حقل جديد غير مرسوم في ORD-06؛ **ORD-09/10 (§٧٧): الزيادة بسبب تُقصّ على المشحون وتُسجَّل، «خلاف مفتوح» حالة قبل سجل DSP، لا حركة مخزون ولا قيد ذمّة حتى LINK-03، الإلغاء بصلاحية `purchase_approve` و«طلب إلغاء من المالك» حدث بلا إشعار؛ **ORD-11/12 (§٧٨): تنفيذ المرتجع (المستند العكسي) LINK/M3 غير مبني، طلب مرتجع واحد معلَّق، الدور 72 ساعة ينقلب مع كل دليل، الإغلاق يعيد الطلب إلى «استُلم»، الوسيط حدث حتى PLT-08، الصور أسماء ملفات؛ **ORD-13/14/15 (§٧٩): `stale` بعد 24 ساعة، المطابقة تخفض `paid_minor` لا قيداً دفترياً، «نسخة الجهاز» = مسودة بمعرّف جديد، الاستعادة يعلنها المالك حتى PLT، قرارات المصالحة توثيقية لا تغيّر القيم؛ **LINK/GROW (§٨٠): صيغة قفل LINK-01 معمَّمة على LINK-02…05، البدائل الحاضرة مُشتقّة من إحالات الإطار، زرّ التفعيل بتسمية «يُفتح مع المرحلة» بدل «تفعيل»؛ **PLT-01/02 (§٨١): تطبيق `stingops` (لا `platform`)، TOTP بلا اعتماديات ونافذة ±30 ثانية، جلسة المشغّل برموز JWT العادية بلا مدة خاصة، «مزامنة متأخرة» ≥ 3 أيام، حجم التخزين بعدد عمليات المزامنة، منح الدعم عبر الـAPI بلا واجهة، لا إيقاف مستأجر بعد** | لا يوقف؛ الأيقونات والمفاتيح والعتاد تُضاف حين تصل |
| 9 | **طلبات الجولة** (`stingops.DemoRequest` من قسم «تواصل» — 0005 §٩٥): في قاعدة البيانات فقط؛ تحتاج شاشة مشغّل وقناة إبلاغ بعد اعتماد مزوّد إرسال (G-02) — ومزوّد الإرسال نفسه يُزيل بطاقة «الرمز الذي كان سيصلك» من التسجيل/الاستعادة | شاشة + مزوّد |
| 8 | **قرار تفعيل السوق** (0005 §٨٧/§٩١/§٩٥): MK-1 وMK-4 ثبتا آلياً → يجوز تفعيل M1 (النشر والاكتشاف)؛ MK-2 ثبت → M2 (الطلبات)؛ MK-3 ثبت (`gate-mk3.spec.ts`) → M3 — **مرفوع في بيئة التطوير فقط** بأمر المالك (`OpsFlag market_m3 env=development` في `sting_dev`)؛ الإنتاج من PLT-12؛ M4 (اقتراح التوريد وتحليلات المورد والترويج، علم `market_m4`) بلا بوابة مستقلة — قرارك؛ تسعير العرض الممول G-04 قرار تجاري خارج الشيفرة؛ `manage.py grow_compute` يحتاج مجدولاً ليلياً في النشر. التفعيل قرارك لا علم تشغيل؛ والتجربة اليدوية §١٥.٥ على جهازين حقيقيين تنتظرك (`docs/evidence/README.md` §5) | تفعيل السوق للمستأجرين |

مؤجَّلات اختيارية مسجّلة في أجسام الطلبات: `default_transaction_isolation=repeatable read` لاتصال PULL في الإنتاج؛ تفعيل Tailwind في `apps/web`؛ صياغة MK-3؛ قيم `23-Handoff` القديمة في الحزمة لم تُعدَّل (لم يُلمَس في الحزمة سوى `tokens.json` و`README.md`).

---

## 5 · خطوات البدء الفعلية للمنفّذ الجديد

1. **تحقق من الحالة**: `git status` نظيف؛ `gh pr list` — لحظة التسليم **لا PR مفتوح** (`main` عند #109). إن وُجد PR مفتوح: ادمجه بعد خضرة فحوصه الأربعة (`gh pr merge N --merge --delete-branch`)؛ سقوط `web` بانقطاع الاتصال = `gh run rerun <id> --failed`. إن اختلف الوضع، أبلغ المالك قبل أي كتابة.
2. **شغّل الفحوص الأربعة في §3** كلٌّ وحده وتأكد من المجاميع (pytest 429 / Vitest 348 (+1 todo) / Playwright 1506 = 502 × 3 / gate 15 — Playwright بـ`--workers=3` وحده وبـ`PLAYWRIGHT_PORT=3011`؛ لا تشغّل الأربعة معاً على آلة واحدة).
3. **اقرأ §1 بالترتيب** (القسم ٣ من الأمر إلزامي كاملاً) ثم 0005 §٨٧ و§٩١ (ما ثبت في بوابات المرحلة ٣ وما ينتظر المالك).
4. **لا مهمة بناء تالية** (المرحلتان ٣ و٣ب مكتملتان — PLAN §٨). ما ينتظر المالك: قرار تفعيل M1…M4 من PLT-12 قرارُه لا علم تشغيل (0005 §٨٧/§٩١)؛ التجربة اليدوية §١٥.٥ على جهازين خطواتها في `docs/evidence/README.md` §5؛ تسعير العرض الممول G-04؛ مجدول `grow_compute` الليلي في النشر؛ الافتراضات المعلَّقة في §4 (3b، 6، 7، 8). المرحلة ٤ (Expo/Tauri) بإذن كتابي فقط (§١٥.١ مرحلة د) — لا تبدأها بلا نصّه.
5. **إن أعطى المالك مهمة جديدة**: فرع `git checkout -b <phase>/<task> origin/main`، والحلقة نفسها لكل شاشة (`frameTextsAll` ← بناء من `@sting/ui-web` بجذر `data-screen`/`data-state` ← Playwright بـ`expectFrame`/`fromFrame` على 390/834/1440 ← commit ← PR ← دمج بعد الخضرة)، ولكل معيار §١٨ مرئي سيناريو في `e2e-gate` وصفّ في ملف الأدلة، وكل غموض ملحق جديد في 0005. قبل امتلاء الذاكرة `/handoff`.

### القواعد التي تُخالَف عادةً بغير قصد (ذكّر نفسك بها كل مهمة)

- النصوص الحرفية تُنسخ **حرفاً بحرف** من الإطار بما فيها التشكيل وعلامات الترقيم؛ لا تُصاغ ولا «تُحسَّن».
- الحالات: فقط الـ17 المسجّلة، وفقط ما هو مرسوم للشاشة في `states-matrix.csv`. لا تخترع حالة.
- النماذج → `saved_local`؛ القوائم → `pending_sync`. النجاح بعد **الحفظ المحلي** لا بعد الطباعة.
- الأرقام لاتينية في IBM Plex Mono مع `direction:ltr; unicode-bidi:isolate`؛ **لا حرف عربي داخل عنصر mono أبداً**.
- الرصيد المركّب: «خادمي X + معلّق هذا الجهاز Y = Z».
- RTL أولاً بخصائص منطقية فقط (لا `left/right`، لا `outline:none`).
- اللمس ≥44px؛ 46px لأزرار POS على اللوحي (`data-pos`)؛ 56px للفعل المالي على الهاتف (`data-financial`).
- لا تقريب في الواجهة؛ التقريب في `packages/domain` فقط. لا حذف أصول؛ المتوقع مخفي حتى إدخال المعدود؛ النقد فقط في الدرج؛ الدفع المختلط بثلاث وجهات مصرّح بها.
- ميزات M3/M4/عقود التكلفة → `phase_locked` (ظاهرة، معطّلة، بسبب).
- لا Noto/Inter/Roboto؛ الخطوط محلية لا CDN.
- القبول = Playwright مطابقة نصية حرفية على المقاسات الثلاثة + أنماط محسوبة مقابل الرموز عند المقاس الأساسي.
