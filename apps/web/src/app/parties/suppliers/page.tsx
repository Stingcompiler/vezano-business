import { SuppliersClient } from "@/features/parties/suppliers-client";

/** PTY-02 — قائمة الموردين (04-D2 ready · 40-D32 loading/empty/permission_denied). */
export default function SuppliersPage() {
  return <SuppliersClient />;
}
