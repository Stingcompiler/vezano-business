import { PrintClient } from "@/features/web/print-client";

/** WEB-03 — ربط طابعة ويب وتجربة عربية (09-D5 / 37-D29: ready/validation_error/server_error/success). */
export default function PrintPage() {
  return <PrintClient />;
}
