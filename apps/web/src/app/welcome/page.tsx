import { WelcomeClient } from "./welcome-client";

/** ACC-01 — ترحيب واختيار الدخول أو إنشاء منشأة (28-D21 ready · 34-D26 offline/server_error). */
export default function WelcomePage() {
  return <WelcomeClient />;
}
