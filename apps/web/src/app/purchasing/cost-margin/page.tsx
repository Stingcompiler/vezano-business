import { CostMarginClient } from "@/features/purchasing/cost-margin-client";

/** PUR-05 — التكلفة والهامش (32-D24 ready/empty/permission_denied/phase_locked). */
export default function CostMarginPage() {
  return <CostMarginClient />;
}
