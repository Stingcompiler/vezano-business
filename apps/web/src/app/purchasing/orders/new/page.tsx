import { OrderNewClient } from "@/features/purchasing/order-new-client";

/** PUR-02 — إنشاء أمر شراء (32-D24 ready/validation_error/saving/success). */
export default function PurchaseOrderNewPage() {
  return <OrderNewClient />;
}
