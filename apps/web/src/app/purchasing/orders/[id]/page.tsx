import { OrdersClient } from "@/features/purchasing/orders-client";

/** PUR-01 — تفاصيل أمر: القائمة نفسها حتى مستند الشراء (PUR-03 — T2.14). */
export default function PurchaseOrderPage() {
  return <OrdersClient />;
}
