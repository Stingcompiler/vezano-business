import { PrefsClient } from "@/features/portal/prefs-client";

/** CUS-04 — التفضيلات وإلغاء الاشتراك لكل المحال المشترَك فيها من هذا المتصفح. */
export default function PortalPrefsPage() {
  return <PrefsClient />;
}
