import { AnnouncementsClient } from "@/features/platform/announcements-client";

/** PLT-04 — إعلانات المنصة وصيانة. */
export default function PlatformAnnouncementsPage() {
  return <AnnouncementsClient />;
}
