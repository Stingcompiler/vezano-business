import { EntitlementsClient } from "@/features/platform/entitlements-client";

/** PLT-12 — إدارة الاستحقاقات وإعدادات التشغيل. */
export default function PlatformEntitlementsPage() {
  return <EntitlementsClient />;
}
