import { OperatorLoginClient } from "@/features/platform/operator-login-client";

/** PLT-01 — دخول الإدارة ومساحة المشغّل. */
export default function PlatformLoginPage() {
  return <OperatorLoginClient />;
}
