import { OverviewClient } from "@/features/platform/overview-client";

/** PLT-00 — لوحة النظرة العامة للمشغّل (بأمر المالك 2026-09-21). */
export default function PlatformOverviewPage() {
  return <OverviewClient />;
}
