import { VerificationsClient } from "@/features/platform/verifications-client";

/** PLT-06 — طلبات تحقق منشآت السوق. */
export default function PlatformVerificationsPage() {
  return <VerificationsClient />;
}
