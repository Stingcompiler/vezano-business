import { PlansCatalogClient } from "@/features/platform/plans-catalog-client";

/** PLT-16 — الباقات والتسعير (بأمر المالك 2026-09-22). */
export default function PlatformPlansPage() {
  return <PlansCatalogClient />;
}
