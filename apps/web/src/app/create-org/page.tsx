import { CreateOrgClient } from "./create-org-client";

/** ACC-04 — إنشاء المنشأة ووصفة القطاع (28-D21 ready · 34-D26 validation_error/saving/success/server_error). */
export default function CreateOrgPage() {
  return <CreateOrgClient />;
}
