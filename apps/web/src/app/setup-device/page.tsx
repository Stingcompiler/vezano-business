import { SetupDeviceClient } from "./setup-device-client";

/** ACC-05 — تجهيز الجهاز والتنزيل الأول (28-D21 offline · 34-D26 loading/partial/stale/server_error/success). */
export default function SetupDevicePage() {
  return <SetupDeviceClient />;
}
