import { CountClient } from "@/features/inventory/count-client";

/** INV-05 — جلسة جرد (05-D2 saved_local · 40-D32 ready/saving/partial/offline/success). */
export default function CountPage() {
  return <CountClient />;
}
