import { NotifyClient } from "@/features/web/notify-client";

/** WEB-02 — إذن Web Push وحالاته (09-D5 / 37-D29: ready/permission_denied/expired/success). */
export default function NotifyPage() {
  return <NotifyClient />;
}
