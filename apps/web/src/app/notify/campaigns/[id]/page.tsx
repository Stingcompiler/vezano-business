import { CampaignsClient } from "@/features/notify/campaigns-client";

/** NOT-03 — تفاصيل حملة (المعاينة والاعتماد في NOT-05 — T2.12). */
export default function CampaignPage() {
  return <CampaignsClient />;
}
