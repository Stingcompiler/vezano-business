import { CampaignsClient } from "@/features/notify/campaigns-client";

/** NOT-03 — قائمة الحملات (17-D12 ready · 36-D28 loading/empty/permission_denied). */
export default function CampaignsPage() {
  return <CampaignsClient />;
}
