import { PreferencesClient } from "@/features/notify/preferences-client";

/** NOT-02 — تفضيلات التنبيه (17-D12 ready · 36-D28 validation_error/offline/success). */
export default function PreferencesPage() {
  return <PreferencesClient />;
}
