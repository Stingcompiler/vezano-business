import { ExportClient } from "@/features/reports/export-client";

/** REP-06 — تصدير تقرير ومعاينته (36-D28 ready/loading/success/server_error · 19-D14 validation_error). */
export default function ReportExportPage() {
  return <ExportClient />;
}
