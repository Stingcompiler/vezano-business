import { StockReportClient } from "@/features/reports/stock-report-client";

/** REP-03 — تقرير المخزون والحركات (19-D14 ready · 36-D28 loading/empty/stale/pending_sync). */
export default function StockReportPage() {
  return <StockReportClient />;
}
