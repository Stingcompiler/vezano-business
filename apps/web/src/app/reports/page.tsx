import { SalesReportClient } from "@/features/reports/sales-report-client";

/** REP-01 — تقرير المبيعات (36-D28 ready/loading/empty/stale/permission_denied · 16-D11 pending_sync). */
export default function SalesReportPage() {
  return <SalesReportClient />;
}
