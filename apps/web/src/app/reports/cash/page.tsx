import { CashReportClient } from "@/features/reports/cash-report-client";

/** REP-04 — تقرير الصندوق والورديات (36-D28 ready/loading/empty/conflict · 16-D11 stale). */
export default function CashReportPage() {
  return <CashReportClient />;
}
