import { ReviewShiftsClient } from "@/features/shifts/review-client";

/** SHIFT-05 — مراجعة فروق وعمليات متأخرة (38-D30 ready/empty/permission_denied/stale · 06-D2 conflict). */
export default function ReviewShiftsPage() {
  return <ReviewShiftsClient />;
}
