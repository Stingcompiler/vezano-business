import { CloseShiftClient } from "@/features/shifts/close-client";

/** SHIFT-04 — إغلاق وعدّ الصندوق (33-D25 ready/validation_error/partial/saved_local/success). */
export default function CloseShiftPage() {
  return <CloseShiftClient />;
}
