import { OpenShiftClient } from "@/features/shifts/open-client";

/** SHIFT-01 — فتح وردية (38-D30 ready/validation_error/saved_local/success · 06-D2 offline). */
export default function OpenShiftPage() {
  return <OpenShiftClient />;
}
