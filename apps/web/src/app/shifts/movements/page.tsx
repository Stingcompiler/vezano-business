import { MovementsClient } from "@/features/shifts/movements-client";

/** SHIFT-03 — حركة صندوق وتسوية (31-D23 ready/validation_error/permission_denied/success). */
export default function MovementsPage() {
  return <MovementsClient />;
}
