import { LockClient } from "./lock-client";

/** ACC-07 — القفل المحلي وفتح PIN (28-D21 ready/validation_error · 34-D26 offline/permission_denied). */
export default function LockPage() {
  return <LockClient />;
}
