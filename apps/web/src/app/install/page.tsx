import { InstallClient } from "@/features/web/install-client";

/** WEB-01 — تثبيت PWA أو عدم دعم التثبيت (09-D5 ready/offline/success/permission_denied). */
export default function InstallPage() {
  return <InstallClient />;
}
