import { PlansClient } from "@/features/public/plans-client";

/** PUB-05 — الباقات والمقارنة (بأمر المالك 2026-09-21؛ ready/loading/offline). */
export default function PlansPage() {
  return <PlansClient />;
}
