import { ImportClient } from "@/features/catalog/import-client";

/** CAT-05 — استيراد أو تعديل أسعار متعدد (38-D30 · 05-D2 partial). */
export default function ImportPage() {
  return <ImportClient />;
}
