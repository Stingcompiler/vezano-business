import { ProbeClient } from "./probe-client";

/** صفحة فحص المرحلة ٠: تثبت الرموز والمكوّنات والتخزين الحقيقي وحالات C-NOTICE — مرجع لمساعد المطابقة (T0.19). */
export default function ProbePage() {
  return <ProbeClient />;
}
