import { OnboardingClient } from "./onboarding-client";

/** ACC-10 — معالج بدء الاستخدام (06-D2 partial · 34-D26 ready/empty/validation_error/offline/success). */
export default function OnboardingPage() {
  return <OnboardingClient />;
}
