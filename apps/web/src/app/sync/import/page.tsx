import { ImportClient } from "@/features/sys/import-client";

/** SYS-10 — استيراد بيانات بمعاينة (16-D11 ready/loading/validation_error/partial/success/server_error). */
export default function ImportPage() {
  return <ImportClient />;
}
