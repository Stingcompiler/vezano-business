import { EpochClient } from "@/features/sys/epoch-client";

/** SYS-08 — تغيّر جيل الخادم والمصالحة (19-D14 stale/conflict/pending_sync/success). */
export default function EpochPage() {
  return <EpochClient />;
}
