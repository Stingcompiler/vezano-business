import { DiscountClient } from "@/features/pos/discount-client";

/** POS-03 — خصم وتجاوز سعر (03-D2 ready/permission_denied · 42-D34 validation_error). */
export default function DiscountPage() {
  return <DiscountClient />;
}
