import { CustomerClient } from "@/features/pos/customer-client";

/** POS-04 — اختيار عميل وإنشاء سريع (03-D2 ready/validation_error · 42-D34 empty/validation_error/permission_denied/offline). */
export default function CustomerPage() {
  return <CustomerClient />;
}
