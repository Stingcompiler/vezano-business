import { SessionsClient } from "./sessions-client";

/** ACC-09 — الحساب والأمان والجلسات (28-D21 ready · 34-D26 loading/permission_denied/pending_sync/success/server_error). */
export default function SessionsPage() {
  return <SessionsClient />;
}
