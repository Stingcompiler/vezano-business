import { UsersClient } from "@/features/org/users-client";

/** ORG-01 — المستخدمون والدعوات (27-D20 ready/expired · 39-D31 loading/empty/validation_error/success). */
export default function OrgUsersPage() {
  return <UsersClient />;
}
