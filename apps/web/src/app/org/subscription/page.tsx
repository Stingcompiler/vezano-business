import { SubscriptionClient } from "@/features/org/subscription-client";

/** ORG-06 — الاشتراك والباقات (27-D20 ready/permission_denied/expired · 39-D31 loading). */
export default function OrgSubscriptionPage() {
  return <SubscriptionClient />;
}
