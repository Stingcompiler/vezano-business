import { ExpiryClient } from "@/features/org/expiry-client";

/** ORG-08 — انتهاء الاشتراك (39-D31 ready/permission_denied · 07-D3 expired). */
export default function OrgExpiryPage() {
  return <ExpiryClient />;
}
