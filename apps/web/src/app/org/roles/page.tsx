import { RolesClient } from "@/features/org/roles-client";

/** ORG-02 — مصفوفة الأدوار والصلاحيات (07-D3 ready · 20-D15 validation_error · 39-D31 permission_denied/success). */
export default function OrgRolesPage() {
  return <RolesClient />;
}
