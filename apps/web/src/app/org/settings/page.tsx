import { SettingsClient } from "@/features/org/settings-client";

/** ORG-09 — إعدادات المنشأة واللغة والقوالب (13-D8 + 27-D20 ready/validation_error/success · 20-D15 conflict). */
export default function OrgSettingsPage() {
  return <SettingsClient />;
}
