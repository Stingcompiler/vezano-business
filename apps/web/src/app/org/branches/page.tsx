import { BranchesClient } from "@/features/org/branches-client";

/** ORG-03 — الفروع وتفاصيلها (39-D31 ready/empty/permission_denied · 15-D10 validation_error). */
export default function OrgBranchesPage() {
  return <BranchesClient />;
}
