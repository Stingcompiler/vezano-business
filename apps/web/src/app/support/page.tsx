import { SupportClient } from "@/features/sys/support-client";

/** SYS-11 — الدعم والتشخيص (19-D14 ready/empty/permission_denied/success). */
export default function SupportPage() {
  return <SupportClient />;
}
